/** Testing: unlimited reprints — all lines every time. Set false after QA for incremental print. */
// TODO(production): enforce per-line copy counts when this is false.
export const PRINTING_FREE = true;
