# Session 2026-08-08

## Shipped

### Consolidated pattern: correct fabric per job / print
- Opening a fabric job's sheet no longer shows another article's fabric
  code (U17/U18 House Thobe case). Links carry `?job=`; sheet-data skips
  "latest job" fallback on multi-article masters; cutter print/PDF expands
  per article when no job is scoped.
- **Print production** also expands one A4 per linked fabric (was stuck on
  one primary code e.g. 206155). Hard-refresh; reopen Print production.

### Restore historical sheets to stored inches
- Filled sizes are inches. Some were labeled `cm` (or converted to cm by the
  Units toggle). Restored: relabel or convert-back to `unit: "in"`.
- Units toggle is display-only now (no storage rewrite).
- On open: auto-heal; sibling empty-sheet heal copies source unit.
- Script: `scripts/relabel-inch-client-patterns.mjs --apply`.
- Live: 46 relabeled + Moussa convert-back to 58.625 in. Ship: `ebe21b5`.

### Site-wide Pattern cm / inches
- Units control on Pattern hub, library, and open sheets (browser preference).
- Display/print/PDF follow the preference; open-sheet toggle also converts
  that pattern's stored numbers when saved. New patterns inherit preference.
- Typed numbers mean the selected unit (cm or inches) - convert on save when
  stored unit differs. Hard-refresh Pattern after deploy.
- Ship: `0c2e27c` (feature) + `564741d` (session SHA log). Both Vercel Ready.

### Moussa House Thobe sheet empty on consolidated pattern
- **Not deleted by Sewing A4 work.** Filled sizes were on duplicate
  `cp-1786026143313-263`; Pattern opens fabric-linked consolidated
  `cp-1785530548344-4379` (`FR-0226-0024-House-Thobe`) which still had 0 rows.
- Restored filled sheet (22 / 19 / 8 remarks) onto the consolidated id.
- Safeguard: opening / assigning fabrics now heals an empty sheet from the
  best same-client + same-garment sibling (`heal-empty-measurements.ts`).
- Pattern: hard-refresh `/pattern/library/clients/cp-1785530548344-4379`.

### Sewing A4s New badge + help
- Amber **New** badge + amber ring on Sewing A4s so Pattern spots it.
- **?** opens a short explanation (vs Print production) and a jump into the picker.

### Sewing A4 pack from Pattern (one page per article)
- Client pattern page: **Sewing A4s** button - tick which articles to include,
  one-click open print preview.
- Print route `?sheet=sewing&lines=id1,id2` renders one A4 per selected
  linked fabric line (article code + fabric + piece QR + measurements).
- PDF download parity for the same query. Protects consolidated patterns
  (e.g. Moussa House Thobes) from printing only the first fabric line.

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| `8e022ab` | Mirror restored inch sheets in pattern-library.json |
| `ebe21b5` | Restore client sheets to stored inches |
| `564741d` | Log site-wide Pattern units ship SHA |
| `0c2e27c` | Site-wide Pattern cm/inches preference (sheets + print/PDF) |
| `c2c48a4` | Heal empty Pattern sheets from filled same-garment siblings |
| `2e1968e` | Document Moussa consolidated House Thobe measurement restore |
| `d7c71fb` | Sewing A4s New badge + ? help tip |
| `e71ec0d` | Fix Sewing A4 controls invalid UTF-8 (Vercel build) |
| `67fbfa9` | Sewing A4 preview with per-article page selection |
