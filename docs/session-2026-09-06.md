# Session 2026-09-06

Production: https://erp.hagan.pro - branch `main`

## Floor nicknames (not legal names)

- Owner: change requests showed PARVAIZ AHMAD KARAM DIN BHATTI. Use the
  short names / nicknames we already have, here and site-wide.
- Dashboard change requests, approvals, scan lookup, badge lists, overtime
  picker, Pattern alteration queue, and new kiosk scans now show
  `short_name` (Parvaiz, Ijaz). Payroll register still has the legal name.
- Ship: `079f137`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Admin select-all on pending decisions

- Owner: as admin there are many stitch change requests to confirm or
  reject (14 pending). Add a Select all button so it is not one-by-one.
- Dashboard stitch kiosk change requests, client name changes, and fabric
  delete queues now have Select all plus Confirm/Reject (or OK/Not)
  selected. Admin/Zapier decide accepts `request_ids`. `/approvals` already
  had select-all.
- Ship: `079f137`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Owner: cannot see Select all. The bar sat in the panel header below
  Today's fabric. Queues now sit at the top of the dashboard with a
  full-width Select all / Confirm all / Reject all bar.
- Ship: `9ed12ce`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Client name on fabric rows (including transfers)

- Owner: fabric rows showed SO + fabric + garment but the client name
  behind the fabric was not readable. When transferring fabric client to
  client, show both names.
- Dashboard stitch change requests and fabric delete queues now print the
  client name on the fabric. Transfer inbound/replacement shows
  `Client B (from Client A)` or `Client A -> Client B`. Same on
  `/approvals`, receiving cuts, transfer modal, Today's fabric reorder
  alert, and compact SO transfer notes.
- Ship: `0698e23`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Start stitching without a printed QR

- Owner: some garments did not have QR ready but stitchers already
  started. Send admin a request with the start time, and let the session
  run even before admin approves.
- Scan tab: QR not ready - start anyway. Session opens immediately with
  the entered start time. Admin `started_without_qr` queue / email.
  Confirm acknowledges; Reject does not stop the work. Stitch kiosk
  employee picker uses the start-without-qr GET (Expats ID list), not
  `/api/hr/employees` (blocked for stitch@).
- Ship: `0698e23`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Boggi Overcoat size runs are Ready-Made

- Owner: SO-2026-0150 Boggi Overcoat (many sizes) is ready-made, not a
  person client. Tell QC and Pattern how to do it next time, English and
  Bangladeshi.
- QC had created two person clients (GL-0926-0014 / FR-0926-0064) with
  Stock-44..68 lines (SO-2026-0142 and SO-2026-0150) plus 26 Pattern jobs.
  Both SOs now sit on Ready-Made / Boggi. Pending pattern jobs cancelled.
  QC order page has Mark as ready-made. How-to emails Pattern + QC.
- Ship: `0698e23`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Boggi brand folder holds the Overcoat size run

- Owner: Boggi is a brand name. Create a folder for it and put the
  overcoat size patterns there. Not a person client.
- Pattern Library Bases already had Boggi / Jacket and Boggi / Trouser
  on Fouad Rahme. The mistaken person-client size sheets (OVERCOAT-STOCK-44
  .. 68) are the ready-made product: one Boggi / Overcoat house base with
  those measurements, file named Boggi Measurement Spec for Overcoat.xlsx.
  Same base on FR and GL. Later a real client loads this base and we change
  his sizes. How-to `howto-boggi-brand-folder-v1`.

## QC corrects stitch start time after a late QR

- Owner: QR was not ready, then stitcher scans badge + garment and the
  scan stamps now. QC must edit that time, send admin a request, and the
  stitcher keeps working even if admin is busy.
- Live/History have Correct start time. The new time applies immediately.
  Admin `correct_start_time` Confirm/Reject does not revert or stop the
  session. QC has Stitch kiosk on the left menu. How-to emails QC + Pattern.
- Ship: `99591f9`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Client dropped off a garment to copy or fix

- Owner: clients leave a ready garment for us to fix or copy. QC / Task /
  Pattern must add it with the full garment-type list, photos (that
  confirms we received it), and later mark that we gave it back.
- Clients page has a Samples view of pieces still in the factory. Open
  the client -> Client garments -> Add garment: type dropdown, Copy or
  Fix, required photos, badge scan. We gave it back to the client sets
  `returned_at`. How-to `howto-client-sample-garment-v1` emails Pattern
  and QC.
- Ship: `bcef9bd`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Delivery dropdown + proof photo

- Owner: add a dropdown on delivery: handed to our factory driver
  (later he will have an account and send a proof picture that he
  delivered to the client) or handed to the client driver directly.
  We can also upload a proof picture.
- Packed production pieces and client drop-off garments use the same
  dropdown + optional proof photo + Sent. Factory-driver account is
  not built yet; `delivery_proof` is reserved.
- Ship: `69421b0`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
