# Session 2026-08-20

Production: https://erp.hagan.pro - branch `main`

## Inventory: Gliani green + Fouad Rahme grey shirt hangers

- Owner: add laundry hangers for Gliani shirts (green) and Fouad Rahme
  shirt hanger (grey).
- Two hanger items: Laundry hanger - Gliani (green) and Laundry hanger
  - Fouad Rahme (grey). Generic Laundry hanger stays. Not swapped into
  shirt pack recipes yet (brand is picked when packing).
- Ship: `a9e1ad2`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Pattern: leftover jobs blocked consolidate (ClickUp retired)

- Owner: Pattern cannot consolidate Ibrahim SO-2026-0130. We are not
  using ClickUp anymore; ERP is the only source of truth.
- Cause: QC already removed some fabrics. Five leftover pattern jobs
  stayed active because a ClickUp guard blocked cancelling them. Those
  rows could not be ticked, so consolidate failed / showed the red
  mismatch banner.
- Fix: leftover jobs for fabrics no longer on the SO are cancelled
  automatically. No ClickUp check. Pattern ticks remaining
  Overshirt+Trouser and consolidates. How-to
  `howto-erp-source-of-truth-leftover-jobs-v1`.
- Ship: `a9e1ad2`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Inventory: 4x6 inch box stickers

- Owner: print a big sticker on each box with all details and a QR.
  Dimension is 4x6 inch.
- Carton print is no longer an A4 sheet of small labels. Each box is
  one 4x6 inch page: item, brand, category, qty, location, notes,
  status, registered date, box id, large QR to /inventory/cartons/[id].
- Print dialog: 4x6, scale 100% / Actual size, do not fit to paper.
- Same open-scan rule: sealed is not stock; scan then Start using.
- Tests: npm run test:inventory (carton-sticker 4x6 guards).
- Ship: `a9e1ad2`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).


## Inventory: laundry bag rolls

- Owner: add laundry bag rolls, long and short.
- Two brandless stock items: Laundry bag roll - Long and Laundry bag
  roll - Short. Unit is rolls. Not wired to pack recipes yet.

## QC: add customer + request name edit (owner ask)

- Owner: QC still cannot edit a name or add a new customer. Add the
  option. Name edit must send a request to admin. Ship and save md.
- QC (client_manager) can add a client from Clients (Add client uses
  the open brand tab) and from /orders/new (always-visible Add new
  client after a production brand is picked). Create is POST
  /api/clients so it does not rewrite the whole list.
- Existing-name edits stay request-only: Request name edit on the
  Clients row and on the order-form picker. Admin approves on
  Dashboard / email link. QC cannot apply a rename.
- Tests: npm run test:name-change (create-client helpers + existing
  request/approve/reject).
- Ship: `bea7be3`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Cherry badge: two cards so all QRs fit

- Owner: 2543411918 all QRs not fitting; print him 2 cards; make sure
  they fit.
- Overflow QRs now split across cards (max 4 per CR80). Cherry's
  finishing pair Ironing + Buttons (+ Washing) stays on card 1; Button
  stitch / Buttonhole / Champa / Bartek print on card 2 at a scannable
  size. Footer shows Card 1 of 2. Reprint Expats ID badges for him.
- Ship: `94d8596`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Pattern: Ibrahim Overshirt+Trouser "fabrics not found"

- Owner: Pattern guy is trying to consolidate Overshirt+Trouser of
  Ibrahim; it shows fabrics not found.
- Cause: leftover pattern jobs on SO-2026-0130 (FR-0726-0037) for
  lines 12-16 (S21006-S21009). Those fabrics are no longer on the sales
  order. Tick-all included them; create pattern 400d.
- Fix: leftover rows are not tickable. Create/assign skips those line
  ids and groups the fabrics still on the SO. How-to
  `howto-consolidate-removed-so-lines-v1` emailed to Pattern.
- If Ibrahim still needs S21006-S21009 patterned, QC must put those
  lines back on the sales order first.
- Ship: `1b3fcdb`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Stitch kiosk: badge then A4 did not finish Asif's overshirt

- Owner sent VIDEO-2026-08-20-12-32-04.mp4. First diagnosis was wrong:
  mid-clip paper scans were treated as A4-only. The opening seconds are
  badge card on the desk, then the A4.
- Stacked Sewing treated the badge as "ready, 1 already open". The A4
  then only entered Closing and asked for the badge again. 30s later it
  dropped back to Sewing (FR-0133-L42-OS).
- Fix: badge then that already-open A4 finishes immediately. Matching
  A4 while already Closing also finishes. Badge alone still arms for a
  NEW A4. Rule `.cursor/rules/floor-video-scans.mdc` so we watch the
  start of floor videos first.
- Ship: `1b3fcdb`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
