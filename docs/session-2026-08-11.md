# Session 2026-08-11

## Paste sizes (pull direction) + copy list reliability

### Why
- Pattern operator still could not get sizes onto the L48 Overshirt rows
  (board screenshot re-sent). Two real causes found:
  1. UX direction mismatch: copy is push-only (source -> targets); he stands
     on the row that NEEDS sizes and wants to pull them in ("paste here").
     Target list shows pattern refs, not article numbers, so he could not
     recognize which target was "L48".
  2. Intermittent backend failure: a forced Supabase read that times out
     returns an EMPTY pattern library, so the copy-targets GET replied
     404 "Client pattern not found" / an empty target list at random.
     Reproduced locally: two identical GETs, one 200, one 404.

### Paste sizes button (new)
- `PasteSizesForm` / `PasteSizesModal` (`src/components/pattern/library/`):
  opened from the row that RECEIVES the sizes. Lists this client's filled
  sheets (same garment or shared piece) as sources, radio-pick one, optional
  piece scope, overwrite / fill-empty, one Paste press. Same API as Copy
  sizes with source and target swapped - all write guards apply.
- Buttons added next to Copy sizes on the Pattern order board rows and the
  pattern job page.
- Copy sizes guide popup bumped to v2 (`hagan-copy-sizes-guide-v2-dismissed`)
  so Pattern sees the new Paste flow explained once.

### Visual "where to press" (v3 guide + New pills)
- Guide popup (bumped to v3, `hagan-copy-sizes-guide-v3-dismissed`) now
  renders a mock order-board row ("L48 - Overshirt") with the **Paste
  sizes** button highlighted (pulse + ring) and "press HERE to pull the
  sizes in" so Pattern sees exactly where the button lives.
- Real **Paste sizes** buttons on the order board rows and job page carry
  an amber **New** pill (same style as other new-feature badges).
- Verified in browser as the pattern account: popup shows the mock row,
  board renders the pill. Bonus: the board's consolidation chip for
  `Abd Nayan-EUP-SS-FR` now correctly reads Overshirt (was serving stale
  "shirt" from the bundled fallback before the degraded-read fix).

### Degraded-read fix (copy-measurements GET, dashboard + v1)
- GET now reads the warm cache first and only falls back to a forced read
  when the id is missing; if the store is empty (degraded Supabase) it
  returns 503 "Sheets are still loading" instead of a bogus 404.
- `CopySizesForm` / `PasteSizesForm`: one auto-retry on failed loads and a
  load-sequence guard so a stale/failed request can never overwrite a newer
  successful one (React strict-mode double effect exposed this).

### Data action
- Pasted Khaled OT 71% wool SUMMERTIME sizes onto the `Abd Nayan-EUP-SS-FR`
  Overshirt master (L44-L48 rows, SO-2026-0133) via the new Paste flow as
  the pattern account: 27 Overshirt points + comments landed, 13 trouser
  rows correctly excluded, zero loss (verified in Supabase).

## Stitchers can chain-stitch several articles at once

- User: a stitcher can be stitching several items together (e.g. white
  shirts chained on the machine), so one-open-at-a-time was wrong for
  Sewing.
- `employeeAllowsStackedOpenPieces` now allows **Cutting and Sewing** to
  stack open pieces; wash/iron and buttons remain one-open-at-a-time.
  Flow: badge -> A4 -> A4 -> ... (one Live session per article, stitcher
  stays armed after each open); close per piece is A4-first (rescan that
  article's A4 + badge). Shared-QR multi-stitcher logic untouched.
- Stacked-open follow-up message is activity-aware ("chain stitch" vs
  "cut pile"). Tests: status-label 16 pass, sewing-session 35 pass.

## Paste dragged the overshirt 1/2 Hem Width onto trouser sheets (fixed)

- User: after pasting, an extra red-circled "1/2 Hem Width 63.2" row showed
  on the Trouser view of `FR-0626-0037-Shirt+Trouser` (real trouser bottom is
  `bottom-width` 19, already present).
- Root cause: the shared id `1-2-hem-width` is dictionary-tagged with
  `trouser` (legacy sheets stored "1/2 Bottom Width" on it) and
  `stitcherPieceAllowList` adds it to Trouser unconditionally, so
  `filterTrialSheetPointsForPiece(source, "Trouser")` - used by the paste
  apply AND the sheet piece view - included the source OT's OVERSHIRT hem row
  and appended it to the target. Tell: only leaked rows carried the
  overshirt's Sample values.
- Fix in `pointMatchesStitcherPiece`: `1-2-hem-width` never matches the
  Trouser piece unless the row NAME says "bottom" (legacy trouser rows keep
  matching via the exclusive-name check). Grouping now puts the top hem under
  Overshirt instead of Shared.
- Data: removed the one leaked row from `cp-1785530548450-3946` (backup in
  `tmp-pdf-inspect/backup-cp-3946-before-hem-removal.json`). The OT sheet's
  own hem row kept - it now displays under Overshirt only (browser-verified
  as the pattern account on both sheets). The leaked "1/2 Waist 62.5" row was
  green-circled by the user as wanted, left in place.
- Tests: copy-measurements 22 pass (new FR-0626-0037 regression);
  measurement-template-mode.test.ts expectations updated (file itself cannot
  run standalone - pre-existing next/server import issue via mutations.ts).

## Drapers swatches showed "No photo" for every non-admin role (fixed)

- Factory laptop (Fabric Specification, Drapers 26130 Cotton Club) showed
  "No photo / No swatch image" - user notes this issue was "solved several
  times" before.
- Root cause: the swatch UI moved to `/api/suppliers/drapers/images` (list +
  image proxy via `drapersSwatchImageUrl`) but that prefix was never added to
  `FABRIC_SWATCH_ROUTE_PREFIXES` in permissions.ts. Middleware 403'd every
  restricted role (pattern / production / QC / sales) on both endpoints, so
  the client quietly fell back to "No photo". Loro Piana / Caccioppoli and
  the OLD drapers medias route were allowlisted - that is why past fixes
  covered those but this one regressed separately. Admins saw images fine,
  which hid the problem.
- Fix: added `/api/suppliers/drapers` to `FABRIC_SWATCH_ROUTE_PREFIXES`
  (spreads into all restricted role allowlists) + the missing routes in the
  permissions swatch-route regression test (28 pass).
- Verified via curl as production / pattern / QC / sales: list endpoint 200
  with items, image endpoint 200 image/jpeg (26130 navy swatch confirmed).
- Follow-up user note "because they are out of stock": verified 26130 is
  actually in stock (47.8m) - the 403 was the only cause. Out-of-stock /
  discontinued fabrics were already never hidden: row Stock column shows
  red "Sold out", modal shows "Discontinued / unavailable in Drapers
  catalog" + warehouse meters, and 1527/1532 catalog fabrics (incl. all 3
  discontinued) have cached swatches. Browser-verified as production.

## Pattern can now upload client photos

- User (screenshot of blocked upload on mobile): "give pattern person also
  the ability to upload images".
- Removed the pattern-only 403 in POST `/api/sales/client-photos`
  ("Pattern operators assign photos to fabrics; Sales uploads them") -
  every `canAccessClientMedia` role now uploads (admin, sales, client
  manager, production, pattern). It was inconsistent anyway: the Aug 10
  mobile post-login prompt invites pattern to upload, then the API blocked.
- Updated the ClientPhotoAssignmentPanel hint copy accordingly.
- Verified as hagan.dp1@gmail.com: POST upload returned 201; test photo
  then removed from sales_workspace + storage.
