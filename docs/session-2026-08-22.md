# Session 2026-08-22

Production: https://erp.hagan.pro - branch `main`

## Pattern: Files tab - who uploaded TUD and other files

- Owner: Pattern and admin need to see which clients did not upload TUD
  and other files. List clients by brand and show who uploaded what.
  Build locally and cloud.
- New Pattern tab Files (`/pattern/missing-files`). Clients grouped by
  house brand. Each sheet shows TUD / DXF / RUL yes or no. Missing TUD
  first. Unlinked jobs show as No pattern.
- How-to `howto-pattern-files-by-brand-v1` emails Pattern and shows on
  the Pattern pages.
- APIs: GET `/api/pattern/missing-files` and
  `/api/v1/pattern/missing-files`.
- Ship: `ea8b713`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Pattern: ERP page is the product, email is extra

- Owner: email is just an extra copy. What matters is building it on the
  ERP so they can see it.
- Queue now has a Files card that opens `/pattern/missing-files`. Pattern
  and admin scan TUD / DXF / RUL there.
- Ship: `185d339`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Pattern: print How-to so there is no excuse he got lost

- Owner: let Pattern print the how-to. Paper at the desk. No excuse he
  got lost.
- A4 print page `/pattern/how-to/print` (optional `?id=` for one card),
  bare print layout. Print all / Print this on How-to and on the banner.
- How-to `howto-print-howto-keep-paper-v1` emails Pattern and shows on
  every Pattern page.
