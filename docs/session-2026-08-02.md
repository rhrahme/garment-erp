# Session 2026-08-02

Production: https://erp.hagan.pro  branch `main`

## Highlights

### Stitch kiosk scanning
- **USB wedge capture hardened:** rapid keys always captured, even over text
  selection or focused search fields; optimistic "Last scan" / "Hearing scan..."
  strip on every tab; Enter and Tab both terminate a scan (`fb22c76`, `a2a6cbf`).
- **Multi-arm queue restored:** `09033b3` (Aug 1 blind-floor recovery) had
  replaced `mostRecentArm` with a "Multiple employees ready" reject; restored in
  `c158f4b` so fast badge->piece scans assign in order again.
- **All Expats ID-list employees allowed** on the kiosk (cutters, wash/iron,
  buttons), not only tailors (`ebcf459`).
- **Job-aware activity labels site-wide:** badge `job_functions` drives
  Cutting/Sewing/etc. on Scan, Live, Performance, History, Orders (`9150331`,
  earlier Live-only `124e584`).
- **Clearer reject reasons:** washing/fabric-cut QR vs production piece QR,
  malformed EMP, placard, AWB; persisted to `sewing_scan_failures` (`849bcb2`).
- **UI:** processed log lines prefixed with HH:mm:ss (`bb947fa`); Scan QR column
  + short names on Live/Performance/History, Piece column removed (`c9e072b`).
- **Scannable QR image first:** the scanned garment QR renders as a crisp SVG
  image before the employee name on Live, History, and Performance pieces so
  supervisors can re-scan straight off the screen (`395c255`,
  `ScanQrSvg.tsx` using the offline vendor generator).

### A4 production print ("PO" sheet)
- Root causes fixed across the day: shrink-to-fit from `max-w-4xl` + 9-col
  table (`74ddfca`), landscape @page + avoid-page traps (`b4d6533`),
  DashboardShell width:auto layout (`7d9cdb1` - moved to bare `(print)`
  layout), Courier/Type3 glyph overlap from font-mono (`b978961`).
- Final recipe recorded in KNOWLEDGE.md. Print dialog: A4 portrait, 100%.

### Supplier emails
- **Email sent / pending pills restored** for QC and all teams on
  `/fabric-orders` and `/orders` - status only, no body (`237c148`).
- **Audit shipped:** `docs/supplier-email-sent-audit.md` (`62e4e12`) -
  48 POs: 16 sent / 32 pending; 5 SOs fully sent, 7 partial, 9 unsent.
  Biggest pending: SO-2026-0116 (68 LP lines), 0111, 0109, 0130.

### Fabric swatches
- Caccioppoli swatch manifest now ships into the Vercel image lambda so SO
  print thumbs render (`b5ac64f`). 206xxx codes still need swatch sync
  (`caccioppoli:download-swatches --fill-catalog`).

### Pattern operator price checkup (hagan.dp1@gmail.com)
- **Full audit, no leaks found.** Every route the pattern login can reach was
  enumerated (middleware allowlist + nav): `/pattern`, `/clients`,
  `/fabric-specification` pages and their APIs. Price-bearing surfaces all
  redact server-side (`/api/pattern/orders/:soId`, `/api/fabric-search`,
  `/api/custom-fabrics`, fabric-spec page props); the rest are price-free by
  data model (pattern jobs/library, client fabric board, fabric change alerts,
  client photos, employee lookup, swatch images). Price unlock code cannot
  help - `canViewPrices` requires admin. Locked in with new tests:
  route-block list + nav assertions in `permissions.production.test.ts`,
  pattern order board payload + unlock-cookie denial in
  `fabric-price-access.test.ts`.

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| `395c255` | QR image column before employee name on Live/Perf/History |
| `c158f4b` | Restore mostRecentArm multi-employee scan queue |
| `9150331` | Job-aware activity labels site-wide from badge job_functions |
| `a2a6cbf` | Always-capture wedge scans + optimistic Last scan strip |
| `fb22c76` | Fix mid-QR chopped scanner input; Last scan on all tabs |
| `62e4e12` | Supplier email sent audit doc |
| `237c148` | Restore Email sent/pending pills for all teams |
| `b978961` | Helvetica/Arial print fonts (fix overlapping glyphs) |
| `7d9cdb1` | Bare (print) layout; lock page width for A4 |
| `b5ac64f` | Ship Caccioppoli swatch manifest to image lambda |
| `c9e072b` | Clean Scan QR column, short client names, drop Piece column |
| `849bcb2` | Explain scan rejects (washing vs production QR etc.) |
| `ebcf459` | Allow all Expats ID-list employees on stitch kiosk |

## Reminders

- 32 supplier POs still pending email (admin action, not code).
- Caccioppoli 206xxx swatches need download/sync before print thumbs show.
- People: Ashraf = cutter-only; Parvaiz `EMP:2631625072` = tailor;
  Abdullah `EMP:2635574938` = multi-role (tailor label wins).
