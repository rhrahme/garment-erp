# Session 2026-08-27

Production: https://erp.hagan.pro - branch `main`

## Inventory Boxes tab

- Owner: add Boxes so they can write how many boxes, how many inside
  each box, print QR, and scan when they open a new box. Then ship.
- Inventory already stored sealed cartons. The Boxes tab is now first:
  add boxes + per-box counts, print 4x6 QR, scan-to-open on the same
  page. Sealed boxes stay off stock until the scan.
- How-to: not Pattern. Clerk lands on /inventory.
- After an opened box is used up they scan the next sealed box. Alert
  button sets the low-stock number (empty = off).
