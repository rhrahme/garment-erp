# Session 2026-08-10

## Shipped

### Cherry dual QR: Ironing + Buttons
- Employees with `wash_iron` + `buttons` (no tailor) print **IRONING**
  (`EMPIRON:{id}`) + **BUTTONS** (`EMPBTN:{id}`) instead of SEWING/ALTERATION.
- Stitch arm persists `activity_job_function`; Live/History show Ironing or
  Buttons for that session.
- USB wedge rejoins EMPIRON / EMPBTN fragments; EMP must not steal them.
- Cherry (`2543411918`) qualifies; Rohan (wash_iron only) stays SEWING/ALTERATION.
- Reprint Cherry from Expats ID badges after deploy.
- Ship: `b2e59c8`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Speed up Pattern ERP
- Sheet GET: one warm-cache library read; heal/seed in `after()`; returns
  `linked_fabric_rows` so Grouped fabrics / Sewing A4s skip full fabric board.
- Slim lists: library index strips measurement grids; client-patterns supports
  `?client_id=` + `?summary=1`; bases picker stays warm-cached.
- Order board / job detail load only that client's pattern summaries.
- Consolidate: bases from `/api/pattern/library/bases`; batch job link
  `POST /api/pattern/jobs/link-client-pattern` (+ Zapier `/api/v1/...`).
- Pattern home drops `?t=Date.now()` cache-bust (keeps `no-store`).
- Writes (Save / assign / heal) still force-fresh + protect/CAS.
- Ship: `3721721`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Cutter stacked multi-open (consolidated nest)
- Cutting activity (e.g. Ijaz) may open many Trouser A4s before closing -
  pile cut on consolidated Khaled OT trousers.
- Open: badge then each A4 (stays armed for next); previous stays open.
- Close: rescan each A4 then badge (A4-first; no badge-first close).
- Tailors unchanged (one open at a time).
- Ship: `cb4c329`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Loro Piana color from swatch
- LP catalog colors were all null; extract dominant color from swatch JPEGs
  into `loro-piana-swatch-colors.json` and show via catalog / SO / Pattern UI.
- Example: 771029 (FR-0133-L26) => Dark brown.
- Ship: `b96924b`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Ops: remove 771020 from cutting history
- Deleted closed Ijaz session `sew-1786348542955-4mwjis`
  (`FR-0133-L18-TR-2/2`, fabric 771020, SO-2026-0133) from
  `sewing_sessions` in Supabase. Sessions 39 -> 38.

### Stitch kiosk change requests (admin approval)
- Stitch/Pattern Live/History/failures: Request stop, edit, delete, pause
  kiosk, or delete failed scan. Admin Confirm/Reject on dashboard queue.
- Store `sewing_session_change_requests`; Zapier + admin email; v1 API parity.
- Protect-write honors approved session/failure deletes.
- Ship: `94cd4b5`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Multi-stitcher same article QR
- Product rule: a garment can be divided across several stitchers on the
  same article QR (jackets/overshirts especially).
- Armed second stitcher starts a parallel Live session; does not close the
  first. Finish = that stitcher's badge then A4 (A4 alone ambiguous if many).
- Ship: `7b17988`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Admin email on new stitch scan
- Email admins when a stitch kiosk opens a new piece session (details:
  employee, QR, fabric, SO, client, kiosk). SMTP failure must not block scan.
- Ship: `191c0a5`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Freeze Live clocks while kiosk paused
- Lunch/admin pause freezes Live/Scan elapsed; pause windows stored in
  `stitch_kiosk_settings.pause_intervals` and excluded after resume.
- Usual factory lunch: 14:00-16:00 Asia/Riyadh.
- Ship: `dd55f3f`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Ops: re-upserted `stitch_kiosk_settings` pause for lunch (doc was missing).

### Mobile post-login: upload client photos?
- Detect mobile client-side; after login, prompt media-capable roles to
  upload client wearing photos (first usual phone task). CTA opens Clients
  with a short upload hint. Once per browser session (sessionStorage).

### Ops: auto-resume stitch kiosk at 16:00 Riyadh
- Lunch pause ended; `stitch_kiosk_settings` set `paused=false`, interval
  closed at 2026-08-10T13:00:00Z (16:00 Asia/Riyadh). Floor can scan again.

### Lunch auto-resume (product)
- Floor scan gate auto-opens at 16:00 Asia/Riyadh after lunch pauses
  (14:00-16:00). Scan/Live heal (not per-article). Manual cron route kept.
- Ship: `439cd61`. Both Vercel Ready.

### Elapsed breakdown (before / lunch / after)
- Live, History, Scan, Performance show work segments around lunch pause,
  not only the work total. Closed duration excludes pause windows.

