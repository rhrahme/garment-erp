# Session summary — 21–24 June 2026

End-of-session backup after Caccioppoli GR Sistemi API integration, Michari/Ajlan customer invoicing, invoice search and fabric-number display, Turki AWB shipment linking (prior), Supabase Auth monitoring, and local Supabase mirror refresh.

## Caccioppoli GR Sistemi API + Vercel token

- **`edb7d0e`** — Live **Caccioppoli API** (GR Sistemi) for stock availability, image lookup, and swatch resolution: routes under `/api/integrations/caccioppoli/*`, `CaccioppoliApiPanel` on suppliers, `useCaccioppoliSwatchMap`, availability sync replacing Esauriti PDF-only updates for Caccioppoli lines.
- **`CACCIOPPOLI_API_TOKEN`** documented in `.env.local.example`; production token set in **Vercel** env (not committed). Optional `CACCIOPPOLI_API_BASE_URL` defaults to `https://api-service.grsis.it`.

## Michari invoice INV-2026-0003 (SAR 10,120 with VAT)

- **`c059981`** — Customer invoice model gains **`vat_rate` / `vat_amount`**; print and editor show subtotal + 15% VAT + total.
- **INV-2026-0003** (Michari Al Kadhi, **SO-2026-0110**): **SAR 8,800** subtotal + **SAR 1,320** VAT (15%) = **SAR 10,120** total, 7 lines, draft in Supabase + local mirror.

## Ajlan invoices (0002, 0004) + search fix

- **`da2637f`** — Invoice list search uses token matching + **Abdulaziz / Abdel Aziz** name aliases so **Abdel Aziz Fahd Al Ajlan** matches phonetic queries; invoices page **`force-dynamic`** avoids stale SSR cache.
- Synced **INV-2026-0004** (and related Ajlan data) from Supabase into `customer-invoices.json`.

## Invoice fabric numbers

- **`9aede7f`** — Customer invoice lines show **fabric number** on list, detail, and print views.

## Turki AWB linking

- **`30347d7`** (20 Jun, included in this push window) — **`OrderShipmentTracking`** on sales/fabric order pages; **`/shipments?awb=`** deep links; **`scripts/link-turki-awbs.mjs`** links DHL AWBs for **SO-2026-0105** / **PO-2026-0003** (Turki).

## Supabase Auth restarts

- Primary monitor remains **`7d4ed8e`** — GitHub Actions **`.github/workflows/supabase-auth-health.yml`** every 5 minutes (see **`docs/supabase-auth-resilience.md`**).
- **`be8b6d2`** (duplicate workflow restore) **not** on `main` — avoided to prevent push/workflow conflicts; daily Vercel cron stays as backup.

## Fabric swatch fixes

- Caccioppoli swatches via API (**`edb7d0e`**) plus prior **Loro Piana / fabric PO** preview fixes (**`012e577`**, **`62894ee`**, **`0b08fba`**).
- Supabase **`sales_orders`** mirror reflects Caccioppoli line **360215** stock → **permanently_unavailable** with **needs_replacement** flags after availability sync.

## Supabase sync (this save)

- `node scripts/sync-documents-from-supabase.mjs` — **19 erp_documents** written locally.
- **`pattern_jobs`** → `src/data/pattern-jobs.json` (**2026-06-21T15:53:04Z**).
- **`fabric_order_drafts`** → `fabric-order-drafts.local.json` (**2026-06-20T17:45:20Z**, unchanged vs prior commit).
- Notable timestamps: **`sales_orders`** **2026-06-23T19:23:46Z**, **`customer_invoices`** **2026-06-23T19:42:30Z**, **`shipments`** **2026-06-23T19:20:29Z**.

Git backup ref: **`refs/backups/pre-save-upload-20260624`** → `b523ef838881314e54b98b11cb5409a1441beea4`.

## Key commits (21–24 Jun 2026)

| Hash | Summary |
|------|---------|
| `da2637f` | Fix Ajlan invoice search; sync INV-2026-0004 from Supabase |
| `edb7d0e` | Add Caccioppoli GR Sistemi API for live stock and swatches |
| `9aede7f` | Show fabric number on customer invoice lines |
| `c059981` | Michari INV-2026-0003 pricing with 15% VAT on invoices |
| `30347d7` | Link Turki fabric AWBs; shipment tracking on order pages |

## Pending

- [ ] **Sticker D550 calibration** — print alignment / rotation (local PDF/PNG artifacts not committed)
- [ ] **Factory two-scan test** — employee session + stage scan on floor hardware
- [ ] **HR workstations** — payroll workspace / employee lookup rollout
- [ ] **Fabric PO availability backfill on production** — `POST /api/supplier-availability-alerts/backfill`
- [ ] **42 missing LP/Solbiati swatches** (carried from 19 Jun)
- [ ] **Drapers API reminder** — local cron `env: node: No such file or directory` in `drapers-api-reminder.log`
- [ ] **Caccioppoli replacement fabrics** — lines flagged `needs_replacement` after API stock sync (e.g. **360215** on Michari order)
