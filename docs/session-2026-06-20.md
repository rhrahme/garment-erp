# Session summary — 20 June 2026

End-of-day backup after supplier availability propagation, factory labels UI fix, Michari invoice create fix, fabric swatch previews, AWB tracking, Turki shipment linking, and Supabase data mirror refresh.

## Supplier availability → fabric orders + sales orders

- **`7b12d87`** — When supplier replies are logged (UI or inbound email), parsed availability/meter updates apply to matching **sales order fabric lines** and **fabric PO lines** via `apply-supplier-availability.ts`.
- Backfill route: **`POST /api/supplier-availability-alerts/backfill`** (and v1 supplier-replies parity) for historical reply data.
- Order list / fabric PO review surfaces show synced availability fields after migration.

## Factory labels visibility fix + safeguards

- **`5bc777c`** — Restored visible **Factory labels** column in fabric add/edit flows (shared `FactoryLabelsField`, `resolveFabricLineLabelCount` in `label-display.ts`).
- Regression script: **`npm run verify:label-counts`** (`scripts/verify-factory-label-counts.mjs`) so mobile and desktop always show label counts.

## Two-scan production tracking (prior session)

- **`47e2489`** — Employee QR session + two-scan stage flow on the production floor (`EmployeeScanSession`, `scan-employee-session`, v1 production scan API, Zapier event).

## Michari invoice create fix (afternoon)

Creating **INV-2026-0003** for **SO-2026-0110** (Michari Al Kadhi) succeeded in Supabase but the UI showed 404 / stale state on Vercel multi-instance.

- **`51f960c`** — Invoice detail, print, and API routes use **fresh reads** (`readCustomerInvoicesFresh`, `getCustomerInvoiceByIdFresh`) so a new invoice is visible immediately after create, even when the write lands on a different instance than the redirect.
- **`ef32c3b`** — Order detail pages (`/orders/[id]`, `/fabric-orders/[id]`) use **`getCustomerInvoiceBySalesOrderIdFresh`**; `CreateInvoiceButton` / `InvoiceableOrdersPanel` treat **409** (invoice already exists) as success and redirect to the existing invoice.
- **`4dddaef`** — Invoice list on Vercel uses fresh reads so new invoices appear without stale cache.

Verified in Supabase + local mirror: **INV-2026-0003**, draft, 7 lines, SAR 736.74, linked to **so-1781953079630**.

## Fabric swatch previews (evening)

- **`0b08fba`** — Swatch previews across order and supplier views.
- **`012e577`** — Sold-out Loro Piana swatch preview fix.
- **`62894ee`** — Fabric order swatch previews on production and list view.

## AWB tracking scan (evening)

- **`e724410`** — AWB barcode scan on the shipments workspace no longer traps navigation; scan resolves and stays usable on the shipments page.

## Turki fabric AWBs + order shipment tracking (night)

- **`30347d7`** — **`OrderShipmentTracking`** on sales and fabric order detail pages; **`/shipments?awb=`** deep links from **`ShipmentsWorkspace`**; **`scripts/link-turki-awbs.mjs`** links DHL AWBs for **SO-2026-0105** to **PO-2026-0003** (Turki).

## Supabase Auth monitoring restart (evening)

Vercel Hobby limits cron to once daily, which disabled reliable auto-restart during GoTrue 522 outages.

- **`7d4ed8e`** (cherry-picked from `c32ae15`) — **GitHub Actions** workflow **`.github/workflows/supabase-auth-health.yml`** runs every 5 minutes as the primary monitor; login retry/backoff tweaks; **`docs/supabase-auth-resilience.md`** updated. Vercel cron remains daily backup.

## Supabase sync (end of session)

Ran `node scripts/sync-documents-from-supabase.mjs` plus pull of `pattern_jobs` and `fabric_order_drafts`:

- **19 erp_documents** written locally (clients, sales_orders, fabric_orders, customer_invoices, supplier_replies, shipments, integration_events, etc.)
- `pattern_jobs` → `src/data/pattern-jobs.json`
- `fabric_order_drafts` → `fabric-order-drafts.local.json`
- Notable remote timestamps: `sales_orders` **2026-06-20T17:08:21Z**, `shipments` **2026-06-20T18:25:54Z**, `fabric_order_drafts` **2026-06-20T17:45:20Z**

Git backup ref: **`refs/backup/pre-save-upload-20260620`** → `b6e720ac`.

## Key commits (20 Jun 2026)

| Hash | Summary |
|------|---------|
| `7d4ed8e` | Restore 5-minute Supabase Auth monitoring via GitHub Actions |
| `30347d7` | Link Turki fabric AWBs; shipment tracking on order pages |
| `62894ee` | Fix fabric order swatch previews on production and list view |
| `e724410` | Fix AWB tracking scan staying on shipments page |
| `012e577` | Fix sold-out Loro Piana swatch previews |
| `0b08fba` | Add fabric swatch previews across order and supplier views |
| `4dddaef` | Fix stale invoice list on Vercel multi-instance |
| `ef32c3b` | Fix stale invoice lookup on order pages; handle 409 on create |
| `51f960c` | Fix invoice 404 after create on Vercel multi-instance |
| `5bc777c` | Restore visible Factory labels field and add regression safeguards |
| `7b12d87` | Sync supplier availability from replies to sales orders and fabric POs |
| `47e2489` | Add two-scan production floor tracking with employee sessions |

## Pending

- [ ] **Sticker D550 calibration** — print alignment / rotation tests (local PDF/PNG artifacts not committed; needs factory printer)
- [ ] **Factory two-scan test** — validate employee session + stage scan on floor hardware
- [ ] **HR workstations** — payroll workspace / employee lookup rollout
- [ ] **Fabric PO availability backfill on production** — run `POST /api/supplier-availability-alerts/backfill` when ready to populate historical PO lines
- [ ] **42 missing LP/Solbiati swatches** (carried from 19 Jun)
- [ ] **Drapers API reminder** — local cron still failing (`env: node: No such file or directory` in `drapers-api-reminder.log`)
