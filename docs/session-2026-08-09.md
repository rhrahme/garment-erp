# Session 2026-08-09

## Shipped

### Trouser measurement points: Entire vs Reduced
- Job Create & open sheet: choose Reduced (default, 17 stitcher points) or
  Entire (full dictionary).
- Sheet UI: Load reduced / Load entire (keeps entered values; reduced drops
  empty clutter).
- API: `measurement_template_mode` on create + rebuild_template PATCH.
- Existing bloated trouser sheets: open and click Load reduced.
- Ship: `96e8868`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Follow-up: Overshirt+Trouser / Suit Reduced keeps overshirt/jacket
  dictionary points + trouser 17 (was trouser-only).
  Ship: `3780fcd` / encoding fix `9d84592`. Both Vercel Ready.
- Follow-up: all compound garments use `getGarmentPieces` for Reduced
  composition (fixes Suit+Vest dropping jacket; covers Shirt+Trouser(+Short)).

### Admin pause stitch kiosk
- Production Floor now: admin Pause / Resume control.
- Blocks floor badge/A4 scans until resumed (shared scan core + Zapier/API).
- Settings doc `stitch_kiosk_settings`; event `production.stitch_kiosk_pause_updated`.
- Ship: `96e8868` (same commit). Both Vercel Ready.

### Pattern stitch kiosk visibility
- `pattern_operator` may open `/stitch` (nav + route allowlist) and use kiosk
  sewing-session APIs; sales/work-order reads match stitch@ restrictions.
- Pause control remains admin-only. Prices still redacted on sales-order APIs.
- Ship: `59f1f0d`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Add fabrics from order on client pattern sheet
- Measurements toolbar: **Add fabrics from order** next to Add trial.
- Modal lists this client's SO fabric lines (This order / Unassigned / All);
  assigns via existing `fabric-lines` POST (+ Zapier parity already wired).
- Also linked from Grouped fabrics card.
- Ship: `3e825d3` (+ encoding fix `918d1fd`). Both Vercel Ready.

### Stitcher sheets: one A4 per piece
- Production / Sewing print for multi-piece garments (Overshirt+Trouser, Suit,
  ...) splits into separate pages per piece QR + filtered measurements.
- Single-piece garments unchanged (one page).

### Cutter sheet: one A4 (all piece QRs together)
- Print cutter no longer opens one page per piece; Overshirt+Trouser nest +
  both floor QRs fit on one A4 (print + PDF). Nest map capped so QR does not
  spill onto a blank second page.
- Also shipped stitcher piece split (production/sewing) with filtered specs.
- Ship: `5fe6453`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Set-garment measurement sheet: piece select
- For set garments (Overshirt+Trouser, Shirt+Short, Suit, ...) Pattern picks
  the piece first, then sees only that piece's measurement rows.
- Single-garment articles unchanged (no select).
- Ship: `003425b`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Custom fabric filing A4 (5x5 cm swatch square)
- Task creates Custom / one-off fabric, then Print A4 filing card with empty
  5x5 cm top-right square to attach a physical swatch.
- Route: `/custom-fabrics/[id]/print`. Also Print A4 on each Custom tab row.
- Ship: `fe82012` (+ encoding fix `074adf2`). Both Vercel Ready
  (`garment-erp`, `garment-erp-kvsf`).

### Pattern order-board batch print
- On Pattern order board: Select all / tick fabrics, choose Production /
  Sewing / Cutter, Print selected -> one preview for all chosen jobs.
- Route: `/pattern/orders/[soId]/print?sheet=&jobs=`.
- Ship: `fe82012` (+ encoding fix `074adf2`). Both Vercel Ready
  (`garment-erp`, `garment-erp-kvsf`).

### CM stamped as inches (site-wide unit fix)
- Bug: auto-consolidate created sheets with default `unit: "in"` while
  Pattern typed CM numbers (no conversion) -> print/display wrong.
- Fix: pass Units toggle into auto/manual consolidate + job/fabric-board
  creates; heal `in`->`cm` when magnitudes look like cm (relabel only).
- Data: Khaled SO-2026-0133 OT SUMMERTIME sheet relabel + job relink
  (`scripts/fix-khaled-ot-cm-unit-relink.mjs`, already applied in Supabase).
- Ship: `fe82012` (+ encoding fix `074adf2`). Both Vercel Ready
  (`garment-erp`, `garment-erp-kvsf`).

### Solid cm/inch clarity (follow-up)
- Sheet toggle shows Show vs sheet stores (amber when they differ).
- TUD/base fill converts when base unit != sheet unit.
- HTML print/PDF both honor `?unit=` (`resolvePatternDisplayUnit`).
- Ship: `0671ebd`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

### Heal must not convert true CM sheets to inches
- Bug: open-sheet heal treated Pattern-typed CM (76/63/66.5) as
  "accidental cm-from-inches" and converted to 29.9375" + unit=in.
- Fix: skip convert heal when cm magnitude band matches; restore Khaled
  OT SUMMERTIME sheet to typed CM (76) in Supabase.
- Verified: 17/17 SUMMERTIME jobs show exact CM; Units=inches converts.
- Ship: `6cd86cc`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Notes

### Cursor iPhone / cloud agents
- GitHub already had `garment-erp`; Remote Control already enabled.
- Smoke cloud agent `MOBILE_OK` launched for phone inbox verification.
