# Session 2026-08-06

## Shipped

### Alteration badge QR + Pattern accountability
- Dual badge QRs: `EMP:` (first make) and `EMPALT:` (alteration).
- Stitch arms alteration via EMPALT; Live/History/Orders show amber Alteration.
- USB wedge rejoins EMPALT fragments; pending queue is idempotent + healed on
  `/pattern`; Zapier/API parity for pending ack / chart updated.
- Docs: `docs/KNOWLEDGE.md` updated. Reprint Expats badges after deploy.

### Pattern stitcher comments on measurement sheet
- Sample / Trials / Final sheet: per-line **Remark** column + bottom
  **Stitcher comments** / sheet notes (print on Production A4).
- Alteration queue on `/pattern`: save stitcher comments (syncs to linked
  client pattern Production sheet). Event
  `pattern.alteration_stitcher_comment`.

### Pattern full ownership of measurement rows
- Add / rename / reorder / remove measurement points on every trial
  (Sample/Trials/Final + Trial detail). Cell edits upsert missing rows.
- Load template points allowed with linked base; rebuild merges onto all
  trials. Helpers: `trial-sheet.ts` sheet-wide ops.

### Badge QR layout fix (size + labels + 3cm gap)
- Dual QRs enlarged to 20mm; labels are full **SEWING** / **ALTERATION**.
- Fixed 3cm clear gap between QR edges (centered pair) - stop maximizing
  gap to opposite card edges. Print CSS locks physical mm sizes.

### Pattern notice: consolidate fabrics then add pattern
- In-app notice on `/pattern` + email to `PATTERN_EMAILS` with step-by-step:
  tick fabrics -> Consolidate selected -> Create / Link pattern -> upload .TUD.
- Store + Zapier parity: `pattern.operator_notice_*`.

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| (pending) | Pattern how-to notice: consolidate fabrics then add pattern |
| `6d48257` | Badge QRs 20mm, SEWING/ALTERATION labels, fixed 3cm gap |
| `aadeeca` | Pattern full add/edit/remove on measurement sheet rows |
| `edb8682` | Pattern stitcher remarks per line + bottom comments |
| `ed7503c` | Sew/Alt badge QRs on opposite edges with wide gap |
| `918f73f` | Alteration EMPALT badge QR + Pattern chart-pending accountability |
