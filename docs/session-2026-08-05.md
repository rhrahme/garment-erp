# Session 2026-08-05

Production: https://erp.hagan.pro - branch `main`

## Highlights

### Two ways to use a base pattern for a client (both live)
1. **Base pattern page** (Pattern library -> open a base): pick the client in
   "Client fit column", tap **Use as base** under a size (e.g. XL), edit the
   amber client column, Save. Stores per-client adjustments on the base chart
   (`client_columns`, shipped `7a56dfc` on Aug 2).
2. **Client pattern job sheet** (Measurements tab): **Load from base pattern**
   -> pick a base + size (or that client's fit column when it exists) -> values
   copy into the **Sample** column. Start the job sheet from a ready-made
   chart; then Save sheet. Shipped today (`1779f43`) with picker preload
   (`0e70c6b`) so the dialog opens instantly.

Same idea, different screens: (1) store the client's fit on the base; (2) fill
Sample on the job so trials can start from there. Unmatched detail points stay
blank when the ready-made chart has fewer rows than the factory sheet.

### "Load from base pattern" fills the Sample column on client sheets
- Feature request (pattern account hagan.dp1@gmail.com) on the client
  pattern measurement sheet (e.g. TROUSER-S05005 / Abdallah Al Luwaihiq):
  a button to pick a base pattern + size and duplicate those measurements
  into the SAMPLE column instead of typing them by hand.
- New "Load from base pattern" button on the Sample / Trials / Final sheet
  toolbar (`ClientPatternDetail`), next to Save sheet. Opens a picker modal
  (`LoadFromBaseModal`): searchable base list defaulting to the sheet's
  garment type (checkbox widens to all garments), then a column choice -
  every size on the base, plus the client's fit column (`client_columns`,
  shipped `7a56dfc`) when that base has one for this client. The fit
  column is preselected when present; otherwise the sheet's derived size.
- Units convert between the base grid and the sheet: cm -> inches snaps to
  1/16" (sheet precision, matches `formatMeasurement`); inches -> cm keeps
  2 decimals; same unit copies untouched. The modal shows a conversion
  note when the units differ.
- Measurement point matching (`src/lib/pattern-library/load-from-base.ts`,
  pure helpers): point_id equality, then normalized-name (case / spacing /
  punctuation-insensitive), then dictionary aliases (both directions), then
  a unique-containment token pass ("1/2 Waist" -> "1/2 Waist straight
  Relux"). Each base point is claimed once; ambiguous rows (e.g. generic
  "Front Rise" when the base has incl./excl. w/b variants) stay empty. The
  modal previews "N of M points will fill" and the result notice lists the
  unmatched names so the operator knows what to enter by hand.
- The copy is client-side into the editable Sample cells (`base_value`
  across trials, same cells as manual Sample edits); persisting goes
  through the existing Save sheet flow (PATCH versions endpoint). No new
  write path, so no new /api/v1 route or Zapier event was needed; the
  existing `client_pattern.updated` fires on save. Pattern operators
  already have access (page + `/api/pattern/library` GET are allowlisted).
- Verified locally on TROUSER-S05005 against Massimo Cotton Trouser 0002
  (cm grid): 22 of 57 points filled, 55.5 cm -> 21 7/8", fractions render
  correctly in the Sample inputs; test fill discarded without saving.
- Tests: `npm run test:load-from-base` (14 cases - conversion rounding both
  directions, normalization, alias/containment/ambiguity/claim rules,
  client-column fallback to its anchor size, full fill run with summary).

### Perf: base-pattern picker preloads - dialog now opens instantly
- User feedback: "loading is taking too long, i think you need to preload".
  Root cause: the picker fetched the FULL pattern library store
  (`GET /api/pattern/library`, 2.5 MB JSON - client_patterns are 1.6 MB of
  it) only when the dialog opened, and the endpoint also waited on the
  clients + sales_orders documents (another ~1.7 MB) loading server-side.
- Fix, three parts:
  1. Slim read: `GET /api/pattern/library/bases` now returns
     `base_patterns` + `dictionary` (218 KB, -91%) and loads only the
     pattern_library document (`ensurePatternLibraryDocLoaded`). Full size
     grids ship in that payload, so picking a base/size never fetches again.
  2. Preload cache: `src/lib/pattern-library/base-picker-cache.ts` -
     module-level cache shared across dialog open/close, 5 min TTL with
     stale-while-revalidate, `invalidateBasePickerCache()` called after
     base create (`PatternLibraryWorkspace`) and base/fit-column save
     (`BasePatternDetail`). `ClientPatternDetail` preloads on mount; the
     TUD-fill picker reuses the same cache (it previously refetched the
     full store too).
  3. Cold-path UX: the modal peeks the cache synchronously for instant
     first paint and shows skeleton rows (not a blank) if it ever has to
     wait. Search stays client-side - no server roundtrip per keystroke.
- Measured (local dev, browser-side): dialog open -> usable base list was
  fetch-bound (2.5 s cold / ~0.2 s warm on localhost; worse in production
  over the network) -> now 78 ms with ZERO network requests; size click ->
  preview 39 ms, also zero requests. Endpoint: 2,514,972 B -> 222,864 B.
- Durable rule: pickers that need base patterns must use
  `/api/pattern/library/bases` + `preloadBasePickerData()`, never the
  full-store `GET /api/pattern/library`, and pages hosting a picker should
  preload on mount.

### Urgent LP chase — PO-2026-0028 / SO-2026-0133 (24 fabrics)
- Sent from `orders.ruh@hagan.pro` to Valentina Cao + Vittorio Osella
  (CC `rhrahme@gmail.com`) asking for status/AWB/ETA on the 24 Summertime
  fabrics with no delay note and not yet received in Riyadh.
- Message-ID: `<e9fc7a76-a393-e7e6-5d3c-f469e894ce55@hagan.pro>`
  (2026-08-05T18:22:46Z). Did not re-mark the PO as newly sent.

### FR-0726-0037 (Ibrahim Al Shwemi) Summertime audit
- SO-2026-0130: 9 Summertime fabric numbers / 11 lines — **none received**.
- Email scan: Solbiati block went as **PO-2026-0020** (Jul 28; Valentina
  confirmed shipment asap). Summertime block in Dubai/Riyadh thread as
  **PO-2026-0027** (only 771025, 771029 x2, 771030 x2, 771040). Valentina
  Jul 31 said PO 21-23-26 and 27 would ship that day.
- Never appeared on any emailed LP order for this client: 771001, 771002,
  771018, 771020, 771022. ERP still has draft `PO-2026-9001` (pending).
- Owner: supplier emails must use **refs only** (FR/SO/PO/line) — no client
  names. Draft for this chase not sent yet.
- Owner: LP lists = fabric no. + print **Code** + labels + meters (not
  garment types). Draft only going forward — owner clicks Send.
- Correction resent for PO-2026-0028 (disregard prior garment-type email):
  Message-ID `<02e0d65d-d9d5-7e11-3a09-2d36559f761c@hagan.pro>`.

### Alteration badge QR + Pattern accountability
- ID badges print **two QRs**: `EMP:` (sew) and `EMPALT:` (alteration).
- Stitch: EMPALT arms next piece as alteration; Live/History/Orders amber
  **Alteration**. USB buffer rejoins EMPALT fragments (not EMP).
- On start: idempotent `pattern_alteration_pending` + Zapier events
  `production.alteration_started` / `pattern.alteration_chart_pending`
  (article + same-fabric siblings; sticker L## article). Soft-fail notify;
  `/pattern` heals + polls. Ack / Chart updated via UI + `/api/v1`.
  Reprint Expats badges.

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| `e6383d1` | Log picker preload perf commit SHA |
| `0e70c6b` | Preload the base-pattern picker so Load from base opens instantly |
| `1779f43` | Load Sample column from a base pattern on the client measurement sheet |
