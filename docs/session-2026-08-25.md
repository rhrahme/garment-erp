# Session 2026-08-25

Production: https://erp.hagan.pro - branch `main`

## Pattern 2 must see Fabric Specification

- Owner: Pattern said the second account cannot see fabric
  specifications. Why. Then ship.
- Cause: Email `pattern@hagan.pro` only received how-to mail. It was
  not a builtin Pattern operator like `production@` / `stitch@`. Badge
  `XX22` already was. Pattern operators never see list prices.
- Fix: `pattern@hagan.pro` is always `pattern_operator`. Lands on
  `/pattern`. Left nav includes Fabric Specification. Prices stay hidden.
  How-to `howto-fabric-spec-both-accounts-v1`.
- Tests: permissions.production, how-to catalog.
- Ship: `ae2a415`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
