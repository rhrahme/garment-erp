# Session 2026-07-08

Production: https://erp.hagan.pro · branch `main`

## Highlights

### SO-0116 supplier email
- **Per-line sent status:** Fabric Order view shows emailed vs pending per fabric line (`bd22159`); Supplier Emails page shows sent vs pending lines (`97d1bdd`).
- **SO-filtered partial sends:** Hardened queue for orders with missing or stale PO links (`8e8db00`, `a311477`).
- **L01–L30 backfill:** `scripts/backfill-so-0116-emailed-lines.mjs` marks historical emailed lines from integration events.
- **PO recovery:** `scripts/recover-so-0116-fabric-po.mjs` restores fabric PO links; integration events now persisted for audit (`9dbd7e5`).

### Supplier inbox scan
- **Upload timeout fix:** Supabase storage uploads use chunked/retry helper; Vercel function timeout extended for scan-inbox (`351721f`).

### AWB tracker
- **Parcels after scan:** Missing shipments backfilled from supplier reply sync after inbox scan (`5c71147`).
- **Supplier name column:** Fabric supplier name shown on AWB tracking table (`b1ea956`, `06e64ef`).
- **Destination city:** Resolved from shipment destination data and shown in tracker (`fad8774`).

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| `fad8774` | Destination city on AWB tracking |
| `06e64ef` | Fix fabric supplier names on AWB tracking |
| `b1ea956` | Fabric supplier name column on AWB table |
| `5c71147` | Fix AWB tracker missing parcels after inbox scan |
| `351721f` | Fix supplier inbox scan Supabase upload timeouts |
| `9dbd7e5` | Persist integration events; harden SO-0116 PO recovery |
| `97d1bdd` | Sent vs pending fabric lines on Supplier Emails page |
| `a311477` | Harden supplier email queue for SO-filtered partial sends |
| `8e8db00` | Fix supplier emails for SO-filtered orders with stale PO links |
| `bd22159` | Per-line supplier email status on Fabric Order view |

## Reminders

- SO-0116 recovery: run `scripts/recover-so-0116-fabric-po.mjs` then `scripts/backfill-so-0116-emailed-lines.mjs` if emailed state is out of sync.
- Supplier email partial send: emailed lines drop off the picker; batch moves to Sent when all lines are sent.
- AWB destination city derives from shipment destination resolution — verify on local/pending shipment rows if city is blank.
