# Session 2026-09-01

Production: https://erp.hagan.pro - branch `main`

## Serwal garment type

- Owner: in the type of garments, add Serwal. Then ship and save md.
- Sales / production / Pattern "garment to stitch" list now includes
  Serwal (one sticker). Measurements, floor color, and library bases
  use the trouser family so Pattern can load trouser points.
- ClickUp serwal / sirwal / salwar imports as Serwal.
- Ship: `feb6b3f`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## HR overtime pay and mistake deductions

- Owner: ship the overtime / mistake money lines. After 10pm they eat,
  then scan to start and stop; hours are the time between those scans.
- Admin **HR & Payroll -> Overtime & deductions** (`/hr/overtime`) now
  stores SAR overtime (optional hours + note) and mistake deductions
  (note + amount + optional photo). This is payroll money, not the
  stitch 22:00 `overtime_confirm` queue. Scan-to-scan overtime hours
  stay on the kiosk path; this page is the pay line.
- Zapier: `payroll.overtime_added`, `payroll.deduction_added`,
  `payroll.adjustment_deleted` plus `/api/v1/hr/payroll-adjustments`.
