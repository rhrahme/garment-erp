# Session 2026-08-04

Production: https://erp.hagan.pro - branch `main`

## Highlights

### Al Ajlan draft invoices prefilled with agreed proposal prices
- User approved "prefill each invoice, i will review them" on the Al Ajlan
  pricing proposal (built by a prior agent from production data, INV-2026-0007
  as pricing base). Five DRAFT invoices now carry the agreed prices in
  production - nothing sent, no client notification, no payments recorded:
  - `INV-2026-0002` (existing empty draft, filled) - Mohamad Abdulaziz Al
    Ajlan Skheikh, SO-2026-0108, DXB no VAT, 21 lines, **65,600 SAR**. The
    A21 Shirt+Trouser pair (LP 771034) was merged into one set line at 6,100
    via the app's suit-combine.
  - `INV-2026-0009` (new) - Abdelaziz Ajlan Al Ajlan, SO-2026-0125, DXB no
    VAT, 16 lines, **81,800 SAR**. Invoiced from SO-2026-0125 ONLY: it
    supersedes SO-2026-0118 (same Caccioppoli + Solbiati PEGASO fabrics,
    corrected meterage), so SO-0118 was intentionally not invoiced (noted on
    the invoice).
  - `INV-2026-0010` (new) - Ajlan Mohamad Al Ajlan, SO-2026-0107, RUH, 18
    lines, 49,300 + 7,395 VAT (15%) = **56,695 SAR**.
  - `INV-2026-0011` (new) - Ajlan Mohamad Al Ajlan, SO-2026-0122, RUH, 5
    lines, 12,200 + 1,830 VAT (15%) = **14,030 SAR**. (App convention is one
    invoice per sales order, hence two drafts for this client.)
  - `INV-2026-0012` (new) - Abdel Aziz Fahd Al Ajlan, SO-2026-0129, DXB no
    VAT, 21 lines, **93,000 SAR**.
- Flagged placeholders for review (invoice lines have no note field in the
  schema, so flags live in the stored invoice `notes` field, prefixed FLAG):
  - INV-2026-0009 A15 / INV-2026-0012 A1: Trouser Zegna 66046 at 3,000 SAR is
    a placeholder (no Zegna catalog price on file).
  - INV-2026-0010 A12: Trouser Drapers 26136 has 4.8 m ordered - possibly 4
    pieces but only 1 invoiced at 2,000; review quantity.
  - INV-2026-0012 A16: Shirt LS Solbiati S25024 at 2,500 SAR placeholder.
  - INV-2026-0012 A19: Shirt SS LP Zelander 781050 is a new garment type
    priced at 2,500 SAR.
- Implementation: `scripts/prefill-al-ajlan-invoices.mts` runs the app's own
  code paths (`buildDraftInvoiceFromSalesOrder`, `buildInvoiceLinesFromSalesOrder`,
  `recalculateInvoiceTotals`, `saveCustomerInvoice`) so numbering
  (`generateInvoiceNumber`), VAT-by-destination (RUH -> 15%), cost hints, and
  Supabase + local JSON persistence stay consistent. Integration events
  emitted like the API routes: `invoice.updated` for the fill,
  `invoice.created` x4 (actor `agent:al-ajlan-prefill`).
- New drafts keep one row per sales-order article (like the app's own draft
  creation and INV-2026-0002) instead of consolidated qty rows: the
  duplicate-line reduction would have dropped real garments sharing a fabric
  number (two T-shirts on LP 722042, four Trousers on Drapers 26136), and
  consolidation renumbers articles / blanks mixed fabric numbers. Reviewer
  can one-click "reduce lines" in the invoice editor after review.
- `scripts/tsconfig-paths-loader.mjs` now also resolves relative
  extensionless TS imports (e.g. `./zapier` inside `src/`), so scripts can
  import app modules whose internal imports are relative.
- Review at `/invoices` (or `/invoices/[id]` per invoice) on
  https://erp.hagan.pro.

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| `8c1be34` | Prefill Al Ajlan draft invoices with agreed proposal prices |
