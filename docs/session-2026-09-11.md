# Session 2026-09-11

Production: https://erp.hagan.pro - branch `main`

## Pr Khaled hint sheet combines matching lines

- Owner: on the hint sheet, combine the same garment type together
  with the same composition and weights so there are fewer lines on
  the invoice.
- Cost hint PDF (invoice, SO, named client pack) now collapses rows
  that share sales order + garment + fibre + gsm. Quantity and
  article count are summed. Fabric numbers are listed together.
  `100% LI` matches `100% linen`. Mill collection names (STREET LINO,
  SUMMERTIME) do not split a group.
- Missing fibre or gsm stays its own row. Seven one-jacket SOs stay
  seven rows. Client quote/invoice PDF unchanged. No invented mill
  prices.
- New invoices from an SO apply the same combine. Existing invoices:
  reprint the hint sheet, or open the invoice and apply the combine
  suggestions.
  Ship: `706d9eec`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Combine three invoices into one

- Owner: combine the 3 invoices into 1.
- Same-client draft invoices now combine into one invoice. The Invoicing
  page shows a Combine button when a client has two or more drafts.
  Ready to invoice can create one draft from several orders of that
  client. Matching garment + fibre + gsm lines stay collapsed. Paid
  invoices are not merged. Client PDF lists every sales order number.
  The combined invoice uses the oldest invoice date.
  Ship: `23c4cbb1`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Combined invoice uses the oldest order date

- Owner: use the oldest date.
- Combining drafts now dates the invoice from the oldest covered
  sales-order `order_date` (July order + drafts created today -> July).
  Creating one invoice from several orders already did this. Opening a
  multi-order draft that still has a newer date writes the older date.
  Due date follows that oldest day plus payment terms.
  Ship: `0546e68d`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Open Pr Khaled invoice without the general dashboard

- Owner: the site root opened Dashboard instead of the new Khaled invoice.
  `/invoices/pr-khaled` opens that invoice when there is one, or Invoicing
  searched to Khaled when there are still several drafts.
  Ship: `c670c59c`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## One invoice covers the orders that were never invoiced

- Owner: the combined invoice showed 26 pcs, not all of Pr Khaled.
- Combine only merged existing **drafts**, so sales orders with no invoice
  yet stayed out. The Invoicing banner now groups a client's drafts **and**
  their uninvoiced orders: "Add N orders to this invoice" with one draft,
  "Combine N invoices + M orders into 1" with several.
- `POST /api/customer-invoices/combine` (+ `/api/v1/...`) accepts
  `sales_order_ids` next to `invoice_ids`. Orders must be the same client
  and not already invoiced. Paid invoices still stay separate. A sales
  order matches its invoice on client code too, so an older `client_id`
  does not block the merge.
  Ship: `ffc0920c` + `c0d18a26`. Both Vercel Ready (`garment-erp`,
  `garment-erp-kvsf`).
- Ready to invoice no longer offers a rival invoice: when the client already
  has one open draft, that green banner says "Add N orders to INV-xxxx" and
  posts to the combine API. Pr Khaled had SO-0131 on INV-2026-0018 with the
  other six orders still uninvoiced.
  Ship: `21eeece4`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- This VM cannot press the button: production invoices live in Supabase
  and only the anon key is here (RLS returns `[]`, no `ERP_API_KEY`).
  The merge is one click in Invoicing.

## "Select at least two invoices." on a one-draft merge

- Owner clicked the merge button and it refused with "choose at least 2",
  while the button itself offered to fold six orders into one invoice.
- `combineCustomerInvoices` always ran `canCombineCustomerInvoices`, which
  bails below two invoices. The API had already been taught that one draft
  plus sales orders is legal, so the request passed the route and then threw
  inside the library.
- `canTakeOnSalesOrders(invoice)` is the one-draft guard: draft, not paid,
  no payments. `combineCustomerInvoices` uses it when it gets a single
  invoice, and both combine routes now share it instead of their own status
  check, so a draft carrying a payment is refused there too.
- A lone draft taking on orders no longer writes "Combined from INV-xxxx."
  into its notes; there is nothing to combine from.
  Ship: `c4523acb`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Merging articles ignored the mill and the money

- Owner: "how can you put Canclini trouser with Zegna and Loro Piana, knowing
  Canclini doesn't have 71% Wool 15% Silk 14% Linen fabrics?"
- The line merge key was garment + fibre + gsm only. Fabric number was never
  in it and `includeFabricBrand` defaulted off, so 17 different Loro Piana
  cloths collapsed into one article, and one worksheet row spanned Zegna,
  Canclini and Solbiati. `ignoreUnitPrice: true` then pulled rows with
  different money together and `mergeConsolidationGroup` wrote price 0.
- The rule is now the owner's: same garment, composition, weight, **mill**
  and **fabric price** merge; anything else keeps its own line.
  `includeFabricCost` joins `includeFabricBrand` in `ConsolidationOptions`,
  and `reductionOptions` turns both on instead of ignoring price.
- `costHintInvoiceGroupKey` gained the mill and all three money columns.
  Rows with no fibre or no weight still key to null and never merge.
- `buildCostHintWorksheetFromInvoice` took a single `salesOrder`, so a
  combined invoice covering 7 orders could only resolve fabric details for
  the first. It now takes `salesOrders[]` and the loader passes every
  covered order.
- "Canclini" on these rows is not Canclini cloth: it is the display alias
  for HAGAN warehouse linen (legacy `gliani-stock` / `gliani-warehouse`).
- Consolidation rewrites and **saves** invoice lines, so invoices merged
  under the old key keep the flattened lines. `POST /api/customer-invoices/
  [id]/sync-lines` rebuilds them from the orders; the invoice page now has
  a "Rebuild lines from sales orders" button because that API had no UI.
- `cost-hint-worksheet.test.ts` was in no npm script; it now runs inside
  `test:consolidate-lines`.
  Ship: `4dc97300` + `9b8133af`. Both Vercel Ready (`garment-erp`,
  `garment-erp-kvsf`).

## One supplier id, two brand names, and a worksheet nobody could read

- After the mill fix one row still merged Canclini with Solbiati.
  `isSolbiatiFabric` returns true for **any** fabric number starting with
  "S", so warehouse "Stock" prints as Solbiati while "BEY 008" on the same
  `supplier_id: canclini` prints as Canclini. The mill key trusted the
  supplier id alone, so they merged. It now keys on supplier id **and**
  printed brand.
- Note the display rule is still loose: `Stock`, `Stock-fabric` and
  `Stock-Fabrick` all show as Solbiati. Changing `isSolbiatiFabric` touches
  orders, receiving, production, stickers and POs, so it was left alone.
- The worksheet repeated all 7 SO numbers, the invoice number and the client
  on every row, which ate the width and forced three-line rows.
  `costHintConstantColumns` finds columns that never change; the view and the
  PDF print them once above the table and drop the column. PDF text went
  6.5pt -> 7.5pt with the freed width.
- `uniqueCostHintSoNumbers` split nothing, so a combined invoice reported
  "1 sales order" for 7. It now splits the comma cell, and
  `costHintNamedClientPackFiles` matches with `costHintRowCoversSoNumber`
  instead of string equality.
  Ship: `d62714e7`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## "Rebuild lines from sales orders" did not rebuild

- The button added earlier posted to `/sync-lines`. `syncInvoiceLinesFromSalesOrder`
  is documented as an **append** operation: it adds articles missing from the
  invoice and explicitly preserves `unit_price` and `quantity` on lines it
  already has, matching by `article_number`. It could not remove the flattened
  lines written by the old merge key, which is exactly what it was recommended
  for. The confirm dialog claimed prices "will be replaced", which was wrong.
- Worse on a combined invoice: `syncInvoiceLinesFromSalesOrders` loops the
  single-order sync per order, and article numbers restart at 1 for every order,
  so order 2's article 1 matched order 1's article 1 and the real line was
  skipped. A 7-order invoice lost lines.
- Added `rebuildInvoiceLinesFromSalesOrders`, which discards `invoice.lines`
  entirely and regenerates from every covered order through
  `buildInvoiceLinesFromSalesOrder` -> `applyAllInvoiceLineReductions` ->
  `renumberInvoiceArticles`, the same path a fresh combined draft uses. New
  routes `/api/customer-invoices/[id]/rebuild-lines` and the v1 twin; both
  refuse an invoice that is paid or carries a payment.
- `rebuild-invoice-lines.test.ts` proves the stale price is gone, that a second
  order's line survives, that two mills stay apart, and - as a regression guard -
  that `sync` really does keep the stale price. 83 tests pass.

## Repo skills

- Added `.cursor/skills/` with `invoice-line-merging`, `ship-to-production` and
  `hagan-erp-guardrails`. Repo skills clone with the repository, so they load for
  every cloud agent with no personal sync setting.
- `.gitignore` changed from `.cursor/` to `.cursor/*` plus `!.cursor/skills/`;
  git cannot re-include a path whose parent directory is excluded.
  Ship: `520c15d9`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Fourth skill: verify-before-recommending

- The rebuild-lines bug was not a merge-rule failure and not a shipping failure,
  so none of the first three skills would have caught it. The cause was wiring a
  button to `sync-lines` because the NAME sounded right, while that file's first
  doc-comment line said "Append... Preserves entered unit prices".
- `verify-before-recommending` covers: never infer behaviour from a name; check
  what a function preserves, not only what it changes; check how it matches
  records across several orders; make the button label and confirm dialog
  describe the code rather than the intention; never recommend an action that has
  never been executed; and when repairing, assert the OLD broken behaviour in its
  own test so a silent revert is impossible.
  Ship: `8c9d56c8`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Price per meter and meters per piece on the worksheet

- Two columns before Fabric cost, on the screen view and the PDF: **SAR/m** (the
  mill price converted from its own currency) and **Meters/pc** (the cloth behind
  one garment).
- Meters are divided by the piece count because `fabric_cost_sar` on a row is per
  piece. Quoting the whole line's meters next to a per-piece cost would not
  multiply out. The subtitle now states the identity:
  `SAR/m x meters/pc + 5% duty = fabric cost`.
- Verified against the seven Pr Khaled orders: **182 of 182** priced lines
  reconcile to the halala. An earlier version rounded meters to 3 decimals in the
  stored value and 5 lines came out 0.06 SAR off, so the value keeps full
  precision and only the display rounds.
- 190 of 221 fabric lines store `unit_price: 0` and keep the real price in the
  supplier catalog. `cost-hint-worksheet.ts` is imported by a client component so
  it cannot read the catalog; `load-cost-hint-worksheet.ts` resolves the price
  through `getSalesOrderCost` and passes `fabricBasisByLineId` in.
- Both columns join the merge key. A merged row prints one price and one length,
  so two rows that reach the same cost from different cloth (200 x 2m vs
  400 x 1m) no longer collapse into one.
- A missing mill price shows blank, never `SAR 0.00`. Non-length units get no
  price per meter at all.

## A test suite nobody was running

- `generate-cost-hint-worksheet-pdf.test.ts` existed but was wired into no npm
  script, and it was **already failing** on `main` before this change: the Swatch
  column was 30pt, too narrow for its own 7.5pt bold header, so jsPDF wrapped it
  to "Swatc" + "h". Column widened to 34pt and the file added to
  `test-consolidate-lines.mjs`. 92 tests pass.
- Roughly 139 more `*.test.ts` files are in the same position - present in the
  repo, run by nothing. Worth an aggregate `npm test`.
  Ship: `b1e6315a`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Four master-level repo skills

- `.cursor/skills/` now holds four skills matching the four ways today went
  wrong, plus `invoice-line-merging` as the one sharp rule.
  - **hagan-erp-architecture** - `src/data/*.json` is a cold-start fallback and
    production reads Supabase `erp_documents`, which a cloud agent cannot reach.
    Plus the document cache and when `*Fresh` is mandatory, role resolution, the
    admin-only `canViewMoney` gate, `api` vs `api/v1` pairing, the client/server
    boundary, and the warning that `npx tsc --noEmit` carries ~262 pre-existing
    errors so it is not a green gate.
  - **hagan-erp-domain** - client codes, fabric line vs label sticker vs article
    (array index is not the article number), the `isSolbiatiFabric` `/^S/i` rule
    and why `supplier_id` legitimately disagrees with the display brand, the
    costing formula with duty and per-piece cost hints, sticker code formats,
    VAT by delivery destination.
  - **hagan-erp-evidence** - never state a live figure read from the repo
    snapshot; never infer behaviour from a function or endpoint name; prove a fix
    before recommending it, and assert the old broken behaviour in its own test.
  - **hagan-erp-change-safety** - fixing a rule does not repair saved data, a
    blast-radius table for shared functions, pricing is the owner's decision, the
    ship procedure, and the duty to give the user the tells that prove they are
    looking at the new version.
- `ship-to-production`, `verify-before-recommending` and `hagan-erp-guardrails`
  were folded into these and deleted.
- Written after mapping the codebase with two exploration passes, one on the
  cross-cutting plumbing and one on the order lifecycle.
  Ship: `4aef3896`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Stale-snapshot mistake worth not repeating

- Asked how many T-shirts the client has, the repo snapshot was read and "3" was
  stated as fact. The live sheet says 7. `src/data/sales-orders.json` was dated
  Sep 7; production had moved on. The same bad source also produced a phantom
  "1 Jacket" and "5 Fabric onlys" that do not exist live.
- The caveat was mentioned and the number was quoted anyway, which is the same as
  not mentioning it. This is now the opening rule of `hagan-erp-evidence`.

## Accounting skill

- `hagan-erp-accounting` covers the money rules that are enforced in code and
  easy to break:
  - **Two legal entities picked by `delivery_destination`**, not by client
    address: RUH issues as Hagan Industries Company in SAR with 15% VAT, DXB as
    Vitasartoria Ltd payable in DHS with no VAT. `bank-details.ts` owns the IBANs
    and nothing else may copy them.
  - `resolveInvoiceVatRate` returns **`null` for Dubai, not `0`** - a zero would
    render a VAT row claiming zero tax.
  - **5% customs duty is permanent and sits inside `fabric_cost_sar`; 15% import
    VAT is recoverable and is deliberately excluded** from garment cost, tracked
    as `vat_recoverable_sar` and `fabric_cash_outlay_sar`.
  - `getInvoiceAmountPaid` treats a legacy invoice marked paid with no payment
    rows as fully paid. That fallback is intentional.
  - Balance due never goes negative, so overpayments do not surface as credits.
  - `SupplierInvoiceRecord.amount` is a **string parsed from a PDF**, not a
    number. Customs `unknown` is not `pending`.
  - The **accounting role is not admin**: it sees the invoicing and supplier
    billing workflow with every amount redacted. New nested money fields must be
    added to `redactSupplierInvoiceMoney` in the same commit or they leak.
  - There is **no ZATCA e-invoicing** in this repo - verified, zero matches. Do
    not claim compliance or invent QR codes for it.
- Claims spot-checked before shipping: no `zatca` anywhere in `src/` or `docs/`,
  `redactCostingOverview` exists at `compute.ts:125`, and
  `supplier-invoices.local.json` is gitignored.
  Ship: `76db1afb`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Asked what other skills the ERP needs. Surveyed the repo rather than guessing:
  324 API routes, 247 components, 163 test files, and **no `.github/workflows`
  at all** - `npm run build` is only `audit:document-loads` plus `next build`,
  so not one test runs automatically. Three subsystems had no skill coverage.
  Two shipped now:
  - **`hagan-erp-side-effects`** - nothing in this repo is a sandbox. `sendEmail`
    has no dry-run, no allowlist and no production gate; `isVercelDeployment()`
    looks like that gate but only guards saving the SMTP password, so a laptop
    with `SMTP_*` set sends real mail. Two routes email **real fabric
    suppliers**. A dashboard page load calls `checkEurSarRateAlert()` and can
    email super-admins. 17TRACK and Zapier are the non-email external writers,
    and Zapier silently returns when `ZAPIER_WEBHOOK_URL` is unset - so a quiet
    local run proves nothing about production. Cron is behind `CRON_SECRET`,
    `/api/v1/*` behind `ERP_API_KEY` (503 when unset), but neither protects the
    session-authenticated supplier send route.
  - **`hagan-erp-printing`** - 14 PDF generators, no shared layout helper, units
    split between pt and mm. Records the column-width failure that shipped the
    split `Swatc`/`h` header, and the measured budgets for the Loro Piana audit
    tables: `renderPartialTable`'s `Missing` header is 29.7pt against a 29.8pt
    budget, **0.1pt of slack**. Also the D550 double-rotation trap, why sticker
    text is rasterised with embedded Roboto, why indexed 1-bit PNG must never be
    embedded, and which failures go blank silently versus throw.
- A subagent's header-overflow list was wrong - it paired headers with another
  table's `columnStyles`. Measured all four tables with `getTextWidth` before
  writing anything down, which is what turned up the real 0.1pt margin.
  Ship: `8d24ba76`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Two more master skills shipped, taking the set to eight plus
  `invoice-line-merging`:
  - **`hagan-erp-factory-floor`** - the sewing session state machine, the
    two-step close and why an unconfirmed `closing` reverts to `open` rather
    than closing, the 2s grace that stops a double-scan closing what it just
    opened, and pause-aware elapsed time versus the admin edit path that skips
    pause exclusion. Corrects a natural but wrong assumption: **sessions do not
    compute pay**. `duration_sec` appears in no file under `src/lib/hr/`; salary
    is imported from a spreadsheet and adjustments are manual. Sessions drive
    Performance, which is not the payslip. Also records that the
    `protect-sewing-*-write` guards are load-bearing scar tissue against a stale
    cache wiping open floor state, and that there is no HTTP idempotency on the
    scan route - all double-scan protection lives in the state machine.
  - **`hagan-erp-pattern-cutting`** - units are the highest-stakes detail in the
    repo: a body length of 76 is centimetres or inches depending on one string
    field. Documents the two `heal-*` migrations, the `looksLikeStoredCmMagnitude`
    guard that stops a genuinely metric sheet being flipped, and the fact that
    **both heals run inside `after()` on the client-pattern GET**, so reading a
    pattern can mutate stored data. Base-to-client inheritance is a snapshot with
    no propagation path. The DXF parser defaults to millimetres. Nesting is
    advisory: `NEST_ESTIMATE_WASTE_FACTOR` never leaves the nest module and
    ordered meters come from the sales order line.
- Both negative claims were re-verified directly rather than taken from the
  subagent reports, since a wrong negative is what misleads.
  Ship: `a319ad08`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Asked whether Zegna `50024` and Loro Piana `771001` really share weight and
  composition. They do not, and the doubt was correct:

  | | Zegna 50024 | Loro Piana 771001 |
  | --- | --- | --- |
  | Composition | 71% Wool, 15% Silk, 14% Linen | 71% Wool, 15% Silk, 14% Linen |
  | Collection | CROSS-PLY | SUMMERTIME (book 771) |
  | Weight | 260 gsm | 250 gsm |
  | Width | 148 cm | 150 cm |
  | List price | USD 137.30/m | EUR 109.00/m |

  The blend ratio genuinely is identical - it is a standard summer suiting mix
  and both mills make one. The weight is 10 gsm apart.
- **Found a real defect while checking.** The Zegna catalog lists fabrics as
  ranges (`50021-50034`), but `findExactCatalogMatch` in
  `resolve-fabric-from-catalog.ts` only accepts exact string equality, so
  `50024` falls through to `buildManualFabricEntry` and returns null
  composition, null weight and **null price**. Ran the real resolver to confirm:
  `zegna/50024` -> `manual: true`, everything null, while the literal range
  string resolves to 260 gsm at USD 137.30.
  **75 of the 80 Zegna catalog rows are ranges, covering 877 individual fabric
  numbers - none of which resolve.** This is the likely source of the "25
  missing fabric price" on Pr Khaled's worksheet.
- The fix already exists unused: `fabricNumberMatchesCatalogEntry` handles ranges
  and is tested, but is only wired into `fabric-catalog-owner.ts`. Trap to handle
  when wiring it in: `toSearchItem` copies `fabric_number` off the catalog row,
  so a naive fix would rewrite the line's number from `50024` to `50021-50034`,
  which would be wrong on the sticker, the PO and the invoice. Not shipped -
  populating prices on 877 fabric numbers changes costing on live orders, which
  is the owner's decision.

- Challenged the worksheet again: the PDF prints `250` for Zegna `50024` while I
  had said `260`, and asked whether `66046` merged with it on L05 is really the
  same cloth. Both objections were right, and the second exposed a shipped error.
- **250 vs 260.** Both figures are real and come from different places. `250` is
  what is stored on the sales order fabric line. `260` is what Zegna's own price
  list says. The price list is production truth (bundled static import); the
  stored value is wrong.
- **L05 and L11 merged genuinely different fabrics.** Per Zegna's catalog:

  | Number | Catalog row | Composition | Weight | Price |
  | --- | --- | --- | --- | --- |
  | 50024, 50023 | 50021-50034 | 71/15/14 wool-silk-linen | 260 gsm | USD 137.30 |
  | 50015, 50017 | 50014-50020 | 71/17/12 | 240 gsm | USD 164.60 |
  | 66044, 66046 | 66044-66046 | 100% Linen | 360 gsm | USD 163.40 |
  | 59215 | 59214-59222 | 62% Silk 38% Linen | 260 gsm | USD 128.80 |

  L11 printed all six as one row labelled "71% Wool 15% Silk 14% Linen 250 gsm".
  `91243`, `59207`, `64003`, `64004`, `64006`, `64011` and `64013` are not in the
  Zegna catalog at all.
- **Root cause is the range defect compounding.** Zegna numbers never resolve
  (75 of 80 catalog rows are ranges), so composition, weight and price were never
  auto-filled; the lines were then filled with Loro Piana's SUMMERTIME figures
  (`71% WOOL 15% SILK 14% LINEN "SUMMERTIME"`, 250 gsm), identically wrong on
  every line. `costHintInvoiceGroupKey` then merged them correctly according to
  its own rule, because fibre, weight, mill and money all matched.
- **Design flaw behind it:** `costHintMoneyKey` maps a missing figure to `"-"`,
  which correctly stops a priced row merging with an unpriced one, but makes two
  unpriced rows match each other. When the price is absent, the only fields
  holding the row together are exactly the ones most likely to be wrong. Recorded
  in the `invoice-line-merging` skill with this case as the worked example.
  Proposed fix: rows with no fabric price must not merge. Not shipped - it splits
  rows on a worksheet actively in use.
- The `Meters/pc` column added earlier is what exposed this: L05 and L06 are both
  Zegna `50024` Overshirt+Trouser and did **not** merge, because 1.75 m/pc and
  1 m/pc differ. L06 at 1 m/pc means 2 m total for an overshirt plus a trouser,
  which is not enough cloth - that line's quantity needs checking on the order.
  Ship: `c389d5f8`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Asked how many metres of Zegna `50024` were received, and where the "2 metres
  total" for L06 came from.
- **The L06 figure was not retrieved from anywhere.** It was arithmetic on the
  user's own PDF: the Meters/pc column reads `1 m`, an Overshirt+Trouser is 2
  pieces, so 1 x 2 = 2. Phrasing that like a looked-up fact was wrong. L06's
  underlying line is not visible in the checked-in data at all - the only 50024
  Overshirt+Trouser there is 3.5 m, which is L05.
- **Could not answer the received-metres question, and said so.** The checked-in
  data files are stamped **4 August**, five weeks before the 11 September
  worksheet - not the four days previously assumed:

  ```
  fabric-receipts / fabric-receipts-archive / sales-orders  updated_at = 2026-08-04
  ```

  As of that date: no receipt for `50024` anywhere across all 1,358 receipt
  records, for any client; Pr Khaled has 23 receipts, 21 Loro Piana and 2
  Solbiati and **zero Zegna**; and `50024` appears in no fabric transfer, no
  inventory record and no production work order. Ordered as of 4 August was
  5.5 m (2 m Jacket on SO-2026-0131, 3.5 m Overshirt+Trouser on SO-2026-0123).
  Reporting "zero received" as fact would have repeated the T-shirt mistake.
- **Every Zegna receipt in the whole history is exactly 1 metre** - all 49 of
  them, across Trousers, Overshirts, Suits and Shirts. That looks like a
  placeholder quantity rather than a real cut length, and it is the same `1 m`
  that appears on L06. Worth checking before trusting any Zegna quantity.
- Confirmed for the user that the two new columns could not have caused the bad
  merges: adding fields to a merge key can only split rows, never combine more of
  them. The wrong compositions predate this session's work.
- Corrected `hagan-erp-architecture` to require reading `updated_at` and quoting
  that date, rather than describing the snapshot as "often days behind".
  Ship: `7778cef0`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Owner rule stated: **two different fabric brands can never merge, whatever the
  weight and composition say.** Checking both merge sites found the worksheet
  already keyed on the mill, but the **invoice did not**:

  ```ts
  /** When true, lines must share fabric_brand to merge. Default false. */
  includeFabricBrand?: boolean;
  ```

  Only `line-reduction-suggestions.ts` switched it on. The invoice route calls
  `applyAllConsolidations(afterSuitCombine)` with no options, so a Zegna line and
  a Loro Piana line sharing garment, composition, weight and unit price merged
  into one article on the customer's invoice. `fabric_brand` is now always in the
  key and the option has been removed, so no caller can turn the rule off.
- Tried a second guard - never merge a row with no cloth price - and **reverted
  it**. Four long-standing tests assert that unpriced rows with the same fibre
  and gsm do merge, which makes it deliberate design rather than an oversight,
  and the owner asked for the brand rule, not the price rule.
- More importantly it was aiming at the wrong target. L05 and L11 are all one
  brand, so no brand rule splits them, and the merge key was never at fault: it
  grouped correctly on the data it was given. **Fixing the Zegna range
  resolution fixes the merging by itself** - once 50024 carries its real
  71/15/14 at 260 gsm and 66046 carries 100% Linen at 360 gsm, the two can never
  land on the same row. Recorded as a test in `cost-hint-worksheet.test.ts`.
  Ship: `76ba461a`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Asked how many meters of 50024 we received. Chased it to the bottom and the
  answer is that **the ERP never records received meters at all.** Receiving
  copies the ordered figure: `fabric_meters: line.quantity`, at four call sites
  in `fabric-receiving.ts` plus `sticker-scan.ts` and the ClickUp importer. The
  scan path never asks for a number - `fabric-receiving-scan.ts` does not
  contain the word `meters`. A receipt is the order quantity with a received
  date on it.
- Worse, `quantity` itself is a mixed column. The ClickUp importer reads a field
  called "Unit" and hardcodes the unit as meters:

  ```ts
  const quantity = getNumber(subtask.custom_fields, "Unit") ?? 1;
  unit: "meters",
  ```

  "Unit" is a garment count. Across the 958 cached ClickUp tasks it is filled on
  92% of fabric subtasks and every value is an integer - 1 x747, 2 x45, 3 x4 -
  including 184 Trousers, 143 Overshirts and 54 Suits all at "1". ClickUp has no
  meters field at all; the importer had nothing right to map. Result: 1169 of
  1886 fabric lines (62%) say exactly 1 meter, and 1149 of 1358 receipts (85%).
  All 49 Zegna receipts in history say exactly 1.
- The worksheet's `1 m` and `1,75 m` were not invented and not received data.
  The column is `Meters/pc` and it divides: `meters / pieces`. SO-2026-0123
  carries a real 3.5 m of 50024 on an `Overshirt+Trouser`, which is two pieces,
  so it prints 1.75. Correct arithmetic, but a number no tailor can read as
  cloth for a garment.
- For 50024 specifically, five lines totalling 10.9 m ordered (5.5 m of it Pr
  Khaled's), all decimals so all ERP-entered and probably true. **No receipt row
  exists for 50024 anywhere in the 1358 receipts** as of the `2026-08-04`
  snapshot.
- Also confirmed the database is reachable but useless from here: the anon key
  is accepted (a bad column name returns a 400 naming `erp_documents`), yet RLS
  exposes 0 tables and returns `content-range: */0`. Every document read in the
  app goes through `getSupabaseAdmin()`, which returns null without
  `SUPABASE_SERVICE_ROLE_KEY`. That key is not in this environment.
- Wrote `hagan-erp-fabric-quantities` so none of this has to be rediscovered,
  and cross-linked it from the costing section of `hagan-erp-domain`.
  Ship: `5e2744e9`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Owner pushed back: the fabric POs we send suppliers show meters per article,
  and the task team confirms receipt, so why can't I pull it? He is right about
  the POs and I had overstated the rest. Corrected both.
- POs **do** carry meters - `quantity_ordered` per line - and it is what goes in
  the mill email (`email-content.ts:169`). But it is copied from the same field:

  ```ts
  // src/lib/sales-orders/create-fabric-pos.ts
  quantity_ordered: line.quantity,
  ```

  So the PO is a real audit record of what we asked the mill for, and it
  survives later edits to the SO line, but it is not independent corroboration.
- The task team's confirmation carries no quantity. `POST
  /api/fabric-receiving/receive` accepts exactly one field,
  `sales_order_line_id`. It is a yes/no that cloth arrived; the system fills the
  ordered figure in behind it.
- Searched all of `src/` for `quantity_received`, `received_quantity`,
  `meters_received`, `received_meters`, `actual_meters` - **zero matches.** The
  three other plausible homes are dead ends too: `SupplierInvoiceRecord` keeps
  only invoice number, amount, currency, AWB and the PDF filename and never
  parses line items; shipments track AWB and parcels; defect types are shade,
  hole, stain, crease, wrong fabric, other, with no "short length".
- Also found the specific reason POs were invisible here, which I should have
  checked before generalising: `fabric-orders.local.json` sits at `process.cwd()`
  as a lazy Supabase document with **no committed fallback** in `src/data/`,
  unlike `sales-orders.json` and `fabric-receipts.json`. Not absent data -
  unreadable data.
  Ship: `00b5fdac`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Owner, correctly and sharply: ClickUp is irrelevant here, the fabric orders
  were placed **from this ERP**, and the ERP is what prints the fabric receiving
  QR. Both points land.
  - His 50024 lines are decimals (2, 1.8, 3.5), which means they were typed into
    the ERP fabric line editor. The ClickUp import defect is real for the older
    imported rows but has nothing to do with his orders, and leading with it was
    noise. Scoped it in the skill with an explicit "check before you raise it".
  - The receiving sticker **does** print meters - `formatStickerCutLength`
    renders `3.5 m` on its own emphasised line
    (`render-sticker-raster.ts:129`). But the value is `cut_quantity`, and
    `qr-labels.ts:160` sets `cut_quantity: line.quantity`. It is the ordered cut
    length, printed as an instruction for the cloth.
  - The QR carries no quantity. `qrScanPayload` returns the production code
    uppercased and nothing else, so a receiving scan transmits `FR-0096-L07` and
    means "the roll with this code is here". The system then writes the ordered
    meters onto the receipt.
- Added the sticker and QR rows to the chain table in
  `hagan-erp-fabric-quantities`, which had skipped the one artifact the owner
  actually handles.
  Ship: `2f2191f9`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

- Owner: "all those mistakes and mess, and we still didnt finish that invoice."
  Correct. Stopped diagnosing and shipped the fix.
- Root cause of the unreadable worksheet, finally nailed. **All of Pr Khaled's
  Zegna lines store `composition: null`, `weight_gsm: null`, `unit_price: 0`.**
  Not wrong data - no data. Price lists abbreviate consecutive numbers into one
  row (`50021-50034`), and `searchSupplierFabrics` matched exactly or by
  substring. `"50021-50034"` neither equals nor contains `"50024"`, so every
  number inside a range fell through to a blank manual entry. 75 of the 80 Zegna
  rows are ranges, covering 877 numbers.
- Fixed in three places:
  - `searchSupplierFabrics` checks range membership after an exact match fails.
  - `findExactCatalogMatch` returns `{ ...inRange, fabric_number: trimmed }` so
    the **entered** number survives. The range is price-list shorthand and must
    never reach a sticker, a PO or an invoice.
  - `buildInvoiceLinesFromSalesOrder` and `enrichInvoiceLinesWithFabricDetails`
    fill fibre and weight from the catalog **only where the line is blank**.
    Anything already stored wins; absent catalog entries stay blank rather than
    being guessed.
- Measured on his real orders (SO-2026-0123 + SO-2026-0131), by stashing the fix
  and running the same probe both ways:

  | | articles | with fibre and weight |
  | --- | --- | --- |
  | before | 20 | 3 |
  | after | 16 | 9 |

  50024 now merges with 50023, and 66044 with 66046, because those genuinely are
  one cloth. 260 gsm wool/silk/linen can no longer share a row with 360 gsm
  linen, which was his L05 complaint.
- Seven numbers - 91243, 59207, 64003, 64004, 64006, 64011, 64013 - are absent
  from `zegna-ss26.json` entirely (it holds no 64xxx or 91xxx at all). They stay
  blank. Not guessing a mill price.
- Found the aggregator's last four imports have been silently dead: a
  `server-only` import throws under the bare node test runner and skips the rest
  of the file. Moved the new test above it and left a comment. 100/100 pass,
  `npm run build` clean, and `tsc` reports the same 262 pre-existing errors
  before and after.
  Ship: `770713fe`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Caccioppoli swatches missing from the cost worksheet PDF

He asked why L08 (Caccioppoli 206202/206204/206205/206206) prints with no
fabric preview when the ERP has a Caccioppoli integration. Three separate
causes, only one of which is a code defect:

- `loadCaccioppoliSwatchJpeg` in `resolve-fabric-swatch-for-pdf.ts` went
  straight to the GR Sistemi API and returned `null` when
  `CACCIOPPOLI_API_TOKEN` is unset. Drapers and Loro Piana both read the stored
  swatch (local disk, then the Supabase `erp-fabric-swatch` bucket) first.
  Caccioppoli did not, so the 1043 jacketing swatches production already holds
  never reached any PDF. `readCaccioppoliSwatchFileAsync` already existed and is
  what the web image route uses; the PDF loader now calls it first and keeps the
  API as the fallback. Fixed.
- The swatch manifest covers 1043 of 1268 jacketing codes and 0 of 614 shirting
  codes. No Caccioppoli shirt fabric has ever been downloaded, so nothing is
  stored for any 203xxx/205xxx/206xxx number. That needs a download run with the
  API token, not a code change.
- 206202, 206204, 206205 and 206206 are not in `caccioppoli-shirting-ss26.json`
  at all. It runs 206101-206493 with gaps, and those four sit in a gap, so there
  is no catalog row to carry specs or a price either.

Also confirms the composition on those lines is wrong, not merely missing. All
312 of the 206xxx shirtings in the catalog are cotton, linen or cotton/linen
around 180 gsm - the immediate neighbours 206219/206220 are 100% cotton popeline
180 gsm and 206221-206228 are 100% linen tela 180 gsm. Not one is wool. The
"71% Wool 15% Silk 14% Linen 250 gsm" on L08 is the same blanket value already
found on ~32 articles across five mills, and it clears on rebuild.

  Ship: `a92df806`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## The price list now overrules stored fabric specs

He asked why it is hard to take the fabric number and pull the specs from the
price lists we uploaded. It is not hard, and it was not happening because of a
rule I wrote yesterday: `fabricSpecForLine` filled blanks only and let anything
already stored win. That is why the wrong composition survived every rebuild.

`catalogFabricSpec` now resolves the number against the mill's catalog and
takes the catalog's fibre and weight. Measured across all 1886 fabric lines in
the committed data: 258 blanks filled, 139 compositions corrected, 18 weights
corrected, 921 unverifiable because the number is in no uploaded list.

Some of the 139 were badly wrong, not cosmetic. Loro Piana 765051 stored
"90% Wool 5% Cashmere" - which does not even add to 100 - against a price list
reading 95/5. 778012 stored "100% Wool" at 260 gsm against 89% wool 11% silk at
350 gsm.

One exception, found by measuring before shipping rather than after. The Drapers
list names fibres without their shares, so 55 lines would have had a real
"97% CO 3% EA" overwritten with "Cotton and Elastane". A catalog value that
states no percentages never replaces a stored one that does.

Two things this deliberately does not do. It does not touch prices. And where
the number is in no price list it changes nothing, so 206202/206204/206205/
206206 stay exactly as they are - the wrong composition on those four is beyond
the reach of any lookup and has to be corrected by hand or by loading the list
that covers them.

  Ship: `1634ae68`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## A spec we cannot confirm now prints blank

He read the previous entry and rejected it: if the price list does not have the
information, the cell stays empty, we do not print something false. He is right
and it is the better rule, so the fallback to the stored value is gone.

`catalogFabricSpec` now has exactly one source. The number resolves against the
mill's list or the composition and weight are null. No stored value is trusted
to stand on its own, because the stored values are where the wrong data lives -
the Loro Piana composition on Caccioppoli shirtings was read out of a stored
line, not invented by any lookup.

Measured over 1886 fabric lines: 965 resolve to a price list, 921 do not, and
806 of those 921 are currently printing a spec that nothing can confirm. Largest
groups are `fr` (316 lines, no price list uploaded at all), `gliani-stock` (253,
aliased to the Canclini linen stock list) and `drapers` (144 numbers absent from
a 1532-row list). That is a lot of blanking, and it is the point: those cells
were never evidence of anything.

Two exceptions survive, neither of them guesswork. The Drapers rule from the
previous entry stands. And a custom fabric keeps its own specs, because a
one-off cloth has no mill price list by definition and its record is the only
one there will ever be.

Side effect worth knowing before the next worksheet is printed: blank fibre or
weight makes `canConsolidateInvoiceLine` and `costHintInvoiceGroupKey` refuse to
merge the line. Unconfirmable articles will each get their own row instead of
collapsing. More rows, but every one of them traceable to a single fabric line.

  Ship: `667a2bf6`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Line-by-line audit of every fabric line in the ERP

He asked for all of Khaled's invoice checked line by line, then every invoice
on the ERP, against the price lists we hold. `scripts/audit-fabric-specs-and-
prices.ts` does it and is read-only; it compares fibre content rather than
wording, using the ERP's own `normalizeInvoiceCompositionKey`, so a mill's
collection name is not reported as a disagreement.

1886 fabric lines across 133 sales orders. 845 agree with the price list, 921
have a number that is in no list we hold, 73 disagree on fibre or weight, 47
disagree on price.

Khaled's seven orders are 221 of those lines: 184 agree, 29 are in no price
list, 8 disagree on price, and **none disagree on fibre or weight**. The wrong
composition he is looking at is not in his sales orders at all - his eight
unlisted Caccioppoli lines store null composition, null weight, zero price, and
today's build produces null for them. It is in the stored invoice rows, written
by an older build, and a rebuild clears it.

All 47 price disagreements are Loro Piana and every one is stored *below* the
current list by two to four percent. That is an old price list against a new
one, not corruption, and whether an invoice bills at the price on the order date
or at today's list is his call. Nothing was changed.

Of the 73 spec disagreements, 21 are the literal placeholder text
"Composition" saved as a composition, 23 are mill shorthand against the same
cloth spelled out (98% WV 2% SE against 98% SUPERFINE WOOL 2% SILK), and the
rest are real: stylbiella 63374/120 stored as 100% Cotton against a list reading
100% WOOL, several Caccioppoli linens stored as 68% cotton 32% linen, stylbiella
50798/206 stored as 70% wool 20% silk with the 10% linen dropped, loro-piana
765051 stored as 90/5 against a list reading 95/5.

The audit also exposed a live hazard, now closed. `findFabricLineForInvoiceLine`
falls back to matching on garment type alone, so an invoice line that matches
nothing by id, sticker or number comes back holding a different article's cloth.
`enrichInvoiceLinesWithFabricDetails` read the mill off that line, which is
exactly how one cloth's composition reaches another's invoice row. It now takes
a spec only when the matched line agrees on the fabric number.

## Drapers: we do have the data, and a prefix was hiding 51 lines

He asked why we lack Drapers data when we are on their API. We are not lacking
it, and my earlier note overstated the gap. Of 1532 rows in the cached catalog,
1490 carry a full percentage composition. Only 42 read as a bare category
("Wool", "Cotton and Elastane"), and every one of those 42 has
`api_detail_updated_at` set - the enrichment ran and Drapers' own API returned
the label without shares. Nothing local to fix there. The rule that keeps a
stored "97% CO 3% EA" ahead of a catalog "Cotton and Elastane" therefore covers
42 rows, not the whole mill.

The real gap was a code format. Cloths get typed in with the mill's own prefix -
"DP 12517", "DP70145" - while the catalog stores the bare five-digit code, and
`findExactCatalogMatch` never normalized it. `normalizeDrapersFabricCode`
already existed for the stock sync and was simply not being used on this path.
Same shape of bug as the Zegna range rows.

`findExactCatalogMatch` now searches the normalized code as well for Drapers and
keeps the number exactly as entered, since the stickers and the supplier PO
already carry the prefix. Across the ERP this pulls 51 lines out of
"not in a price list": 921 unmatched down to 870, and 845 agreeing with the list
up to 892. Two numbers, 90639 and 90642, are genuinely absent from the catalog
even without the prefix and stay unmatched.

  Ship: `76806c05`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## No rebuild is needed - every view re-derives specs on load

I told him repeatedly to click "Rebuild lines from sales orders" before the
corrected specs would appear. That was wrong, and it is worth writing down
because it sent him chasing a button he never needed.

All three places an invoice is read call `enrichInvoiceLinesWithFabricDetails`
at load time, not at save time:

| Path | Call site |
| --- | --- |
| Invoice page | `app/(dashboard)/invoices/[id]/page.tsx:56` |
| Client quote / invoice PDF | `invoicing/prepare-invoice-document.ts:34` |
| Cost hint worksheet | `costing/load-cost-hint-worksheet.ts:57` |

So the stored line is only a starting point; composition and weight are resolved
against the price lists on every single load. Until today that did not help,
because the stored value won. Now the price list wins, and the same load path
corrects the row on the way to the screen.

Proved it rather than assuming. Took his seven real orders, built the 216
invoice lines, then deliberately poisoned every one of them with
`71% WOOL 15% SILK 14% LINEN "SUMMERTIME"` at 250 gsm - worse than production
actually is - and ran the worksheet load path over them. 86 rows out. The eight
unlisted Caccioppoli numbers come out blank. 47 rows carry a composition
confirmed by a price list. The only ten rows still reading SUMMERTIME are the
genuine Loro Piana 771xxx, where it is the correct answer.

A rebuild is still worth doing eventually, because the stored rows in Supabase
are themselves wrong and anything reading them raw would see that. It is not
needed for the invoice, the quote or the worksheet.

  Ship: `f9cfc7eb`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Per-client pass over all 53 clients

He asked for every client's invoice checked line by line, so the audit script
grew a `--by-client` mode that groups by client, names the orders and any
invoice in the snapshot, and prints each disagreeing line underneath.

1886 lines, 53 clients, 133 orders. One split matters before reading any of it:

- Ready-made brand accounts (`RM-` codes: Suit Supply, Massimo Dutti, Boggi,
  Lebanon Beirut, Zegna, Luca Faloni, Cafe Cotton, Blue Mint) are 463 lines, and
  454 of them resolve to nothing. That is correct. Their fabric numbers are
  literally "Stock" against `gliani-stock`, `fr` and `unknown`. There is no mill
  price list for warehouse stock and there should not be one. These are not
  errors and must not be counted as any.
- Bespoke clients are the remaining 1423 lines, of which 416 are in no price
  list, spread over 192 distinct numbers - the biggest groups being
  `gliani-stock` (38), `fr` (28), loro-piana (25), drapers (22).

Worst first by number of wrong specs: Abdullah Al Moussa (21 wrong specs, 11
price differences over 151 lines), Abdeliah abou Nayan (11), Mazen Al Zahrani
(8), and Rakan, Raddad and Salman Al Zahrani (7 each). Moiid Al Zahrani and
Samer Al Jameel are almost entirely price differences - 13 of 14 and 8 of 9 -
and every one of those is the old Loro Piana list against the new one.

Nineteen clients come back completely clean, including every Al Ajlan account,
Ibrahim Al Shwemi, Youssef Al Rashed and both Luwaihiqs.

Khaled: 221 lines, 184 agreeing, 29 in no price list, 8 price differences, and
no wrong specs at all.

  Ship: `440dbe9e`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Auditing everything, not just fibre and price

"if khaled had mistakes, means toher clients has also mistakes." Fair, and my
audit had only ever looked at two fields. Widened it to the other shapes an
error can take. 1886 lines, 133 orders.

Two of my own checks were wrong on the first run and were measured out before
they shipped, which is worth recording because both failures were the same
mistake in different clothes - assuming instead of counting.

- A median metreage baseline flagged the *correct* lines. Suit holds 196 lines
  at 1 m against 18 at 3 m, so the median metreage for a Suit IS the bad value,
  and measuring against it accuses the three-metre lines.
- One-character matching on fabric numbers gave 683 pairs, nearly all real:
  `GEC 720` and `GEC 620` are two different cloths. Only a differing *letter*
  is a typo, which is where `Stock-Fabric` / `Stock-Fabrick` lives.

### What it found

**Nineteen sticker codes carry two different cloths each.** This is a real bug
in the ERP, not bad typing. Appending a fabric line counted the next `L##` from
`order.fabric_lines.length`. Deleting a line shortens that array but does not
un-print the sticker that left with it, so the next append was handed a number
another line was still wearing. SO-2026-0130 issued 27 articles, lost 5, and
the next line appended came back as L23 - which the surviving S14023 already
held. Six orders affected: SO-2026-0130, 0132, 0125, 0119, 0115 and 0002.

Fixed by counting from the highest article any sticker carries, which is the
rule `transfer-fabric.ts` already used for the same reason. Nothing already
printed is renumbered.

No invoice is wrong because of this. `findFabricLineForInvoiceLine` resolves by
`sales_order_line_id` first, and all 141 stored invoice lines carry one; zero
resolve through an ambiguous sticker. The exposure is the production floor,
where the code is what gets scanned. Repairing the 19 stored collisions is not
mine to do - it means either reprinting stickers or leaving physical labels
that no longer match what the ERP says.

**Six priced lines sit at exactly one metre.** All on SO-2026-0008, Abdel Aziz
Fahd Al Ajlan, Drapers 60xxx vests at 43.10 and 50.10 a metre. These multiply a
real price by a placeholder, so the cost worksheet carries a wrong number.

That one metre is everywhere else too - 1169 of 1886 lines, 62% - but it is not
an ERP fault and, apart from those six, costs nothing: the order form refuses an
empty metreage, and 1163 of the 1169 are unpriced, so there is no price to
multiply. It is a habit that faded on its own: 537 of 542 lines in January, 0 of
160 in June, 36 of 413 in July. Reported as context, not as 1169 errors.

**Ninety-four lines carry the literal word "Composition"** where fibre content
belongs, across 17 clients. **Thirty-one carry a placeholder fabric number** -
`Stock`, `Client Own Fabric`, `TBD` - after excluding the `RM-` ready-made
accounts, where `Stock` is the true answer.

**Eight fabric numbers are one letter from another at the same mill.** Three are
plain typos: `Stock-Fabrick` (Khaled, SO-2026-0116), `Old Fabrick` and
`Old fabrick`. Four are the same missing-prefix problem the Drapers `DP` fix
solved - `1415541-1` against `C1415541-1`, `1415541-2` against `C1415541-2`,
`1436697-2` against `C1436697-2` - one cloth typed two ways. `25H003`/`25T003`
and `T003`/`H003` are probably two real cloths and need a human eye.

Khaled is clean on all of this except the one `Stock-Fabrick`: 221 lines, 3 at
one metre and none of them priced, no reused stickers.

  Ship: `0b1843b6`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## The invoices themselves

Everything above audits the sales orders. The invoices had not been checked as
documents - the arithmetic, the VAT, the due dates, the payments, and whether
any order is billed twice or not at all.

Checked against the ERP's own `resolveInvoiceVatRate`, `computeDueDate` and
`getInvoiceAmountPaid` rather than a second implementation of the same rules,
so a disagreement means the stored row is stale rather than that I read the
rule differently.

**The arithmetic is clean.** 12 invoices, 141 lines, zero disagreements. Every
`line_total` equals quantity times unit price, every subtotal equals its lines,
every VAT amount equals subtotal times rate, every total equals subtotal plus
VAT. Every VAT rate matches its delivery destination - 15% on RUH, none on DXB.
Every due date matches its payment terms. No duplicate invoice numbers, and no
sales order appears on two invoices.

One row is worth naming: **INV-2026-0006 (Blair Maxwell) carries 13 articles and
totals zero**, because all 13 lines are unpriced. Safe as a draft and wrong the
moment it is sent. Those 13 are also the only unpriced lines in the whole set -
the other 128 all carry a price.

Two of my checks were wrong before they were right, both from assuming a rule
instead of reading it:

- `resolveInvoiceVatRate` returns `null` outside Saudi, not `0`, and invoices
  store that same `null`. Comparing them unnormalised produced the nonsense
  "VAT rate 0% stored, but DXB calls for 0%" on five invoices.
- Three paid invoices have an empty `payments` array, which I read as a missing
  payment. It is a supported state: `getInvoiceAmountPaid` counts a legacy
  fully-paid invoice with no ledger as paid in full, which the comment there
  says outright.

**Scope limit, stated plainly.** This covers the committed snapshot only, which
ends 2026-08-04. INV-2026-0018 is not in it, and neither are 121 of the 133
orders - including all seven of Khaled's, which is exactly why his invoice was
raised after that date. Production truth is Supabase, and this VM holds only the
anon key, which RLS correctly refuses rows to. Auditing the live invoice needs
a service role key or `ERP_API_KEY`, and neither reaches an already-running
agent.

  Ship: `c7463de5`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Sobhan: firing someone the ERP had no way to fire

"sobhan is fired, so you can remove him from all and disable."

He is `MOHAMMED SOBHAN SOBAN BABRI BAPARY`, employee id `2635574383`, short
name "Ab Sobhan". One record, one file. There is no second employee store: the
badge, kiosk, stitch floor and payroll screens all read `payroll_employees`.

`is_active: false` is already the right lever and it is already wired
everywhere that matters. Setting it blocks badge login, the kiosk employee
lookup, starting a sewing session, appearing on the stitch floor dashboard or
the manual-start list, badge printing, the employee list API, payroll
adjustments, and the payroll total.

**The ERP had no way to set it.** `is_active` was written `true` at record
creation and never touched again. The admin PATCH route accepted only
`assigned_workstation_id`, `is_mobile_floater` and `job_functions`. So the
answer to "disable him" was not an edit, it was a missing feature.

Built it: `is_active` on the patch type and the admin route, an
`employee.updated` integration event so there is a trail of who did it, and a
confirm-guarded "Mark as left" in the payroll workspace with a "Show N who
left" toggle and "Reinstate", so a mis-click is recoverable.

Deactivate, never delete. The record still has to answer for final settlement,
past WPS transfers and production work already attributed to him. Marking
someone as left releases their workstation and floater flag so the bench frees
up, and keeps their job functions, which do nothing while inactive and are
worth having on a rehire. The merge is a pure function so those rules are
tested without touching the store.

Found alongside it: the payroll search box crashed. It maps job functions
through `EMPLOYEE_JOB_FUNCTION_LABELS` and never imported it, so typing threw
a `ReferenceError`. `typescript.ignoreBuildErrors` kept it out of the build.

**What I could not do.** I could not disable him myself. `payroll_employees`
lives in Supabase and this VM holds only the anon key, which RLS correctly
refuses. The committed fallback now says he has left, but production does not
read it. An admin has to open HR > Payroll and click "Mark as left".

  Ship: `aac97e6b`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## "Have you verified each invoice, line by line, of all clients?"

Answered with a measurement rather than a recollection. New script,
`scripts/audit-invoice-fabric-specs.ts`, renders every line of every invoice
the way the client's PDF renders it, then checks that against the mill price
list. The order-level audit reports what is *stored*; this one reports what is
*printed*, and they are not the same thing.

12 invoices, 141 lines, every client in the snapshot:

- 132 lines print a composition and weight taken straight from the price list.
- 9 print blank.
- **0 print a spec no price list backs.** The rule in `catalogFabricSpec` holds
  under test: the list always wins, and an unknown cloth blanks both cells.
  Invented specs can no longer reach a client invoice.

The blanks split into two different jobs. Two are *misfiled*: `10005.169/22`
and `10015.210/2` are tagged Canclini and Gliani stock, but the Wool Stock list
holds both numbers verbatim, and the stored composition already matches that
list. Correcting the supplier fills them in. Seven are *absent*: no uploaded
list holds the number at all, and no editing will fill them.

**One real fix came out of it.** The Canclini stock list files a cloth under a
bare number and records the mill's own pattern code beside it - row
`1422477-1`, description "pattern C11422477-1". Orders are written with the
mill's spelling, so the lookup failed and the invoice printed blank for a cloth
the list describes perfectly. The pairing is stated by the price list, so
reading it is not guesswork. Nine order lines now resolve that did not before.

The guard matters more than the fix. Two Canclini rows both call themselves
C11422487-1, at 5.30 and 5.23, and one is also literally numbered that. Picking
a price there is the owner's decision, not a lookup's, so a code is only
indexed when exactly one row claims it and no row owns it outright. Contested
codes keep printing blank. I nearly shipped this without the uniqueness check.

**Scope, stated plainly.** This is the committed snapshot, which ends
2026-08-04 at INV-2026-0012. Production has at least six more invoices,
including Khaled's INV-2026-0018, and this VM cannot read them: they live in
Supabase and the anon key is correctly refused rows. So "all clients" means all
twelve invoices that exist here, not every invoice that exists.

Also found, and left alone deliberately: 47 lines priced below today's Loro
Piana list, 77 with a stored composition the list disagrees with (overridden at
render, so no client sees them), and the duplicate Canclini rows above. All
three are pricing or data decisions for the owner.

  Ship: `258fb521`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## The nine drafts, line by line

`scripts/audit-draft-invoices.ts`. Drafts only: nothing sent, nobody paid, so
everything here is still cheap to fix. 130 lines across nine invoices.

Every line resolves to its own order line by id - none fell through to the
garment-type guess at the end of the match chain, which is the failure mode
that hands a line another article's cloth. All nine add up: line totals,
subtotals, VAT and totals agree everywhere, and VAT matches destination on all
of them.

**INV-2026-0007, Abdelaziz Mohamad Al Ajlan, 136,100.** The real one. The order
holds 39 garments; the invoice bills 35. Four are made and not charged for: an
Overshirt in S23036, two Shirt LS (781060, 781056) and a Short in 772003. And
article 12 bills 772003 as a "Shirt + Trouser + Short" at 8,200 while the order
cut that cloth as an "Overshirt + Trouser", with the Short split onto its own
line on 2 August. So one garment is described and priced as something other
than what was made, which is also why the counts disagree.

**INV-2026-0006, Blair Maxwell.** All 13 articles priced at zero, no delivery
destination, so VAT cannot be decided either. Unsendable as it stands.

**INV-2026-0010, Ajlan Mohamad Al Ajlan.** Drapers 26136 appears on four lines
and prints two different compositions: "98% CO 2% EA" on one, "Cotton and
Elastane" on three. Both come out of the existing rule, correctly applied. The
catalog row names the fibres without their shares, so a stored composition that
states them wins - but three of those lines have nothing stored, so the vaguer
catalog text stands. One cloth, one page, two descriptions. The fix belongs in
the Drapers catalog entry, not the renderer, and which text is right is not
mine to decide.

**INV-2026-0009, Abdelaziz Ajlan Al Ajlan.** Sticker
`...SO-2026-0125-L13-TSH` is on two lines carrying different cloths, 722056 and
722043. One of the nineteen sticker collisions, and the first seen reaching an
invoice. The money is right - both T-shirts are billed once at the correct
price, and both resolve by line id - so the damage is two physical garments
wearing one scannable code.

Three cloths still print blank: `C1411635-1` and `C11513221-1` on Blair
Maxwell, `780001` on INV-2026-0007, `S25024` on INV-2026-0012. No uploaded list
holds any of them.

**A check of mine was wrong before it was right.** Counting invoice lines
against order lines reported sixteen unbilled garments on INV-2026-0007.
Consolidation merges several order lines into one line carrying a quantity and
keeps only one of their ids, so thirteen of those sixteen were billed inside a
quantity. Counting garments per type instead gives the true four. It also
cleared a phantom finding on INV-2026-0001, whose two lines match by sticker
rather than by id.

  Ship: `1adfb6aa`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## SAR/m and meters on every hint invoice, not just Pr Khaled's

- Owner: put the price per meter and the meters used on the cost hint for all
  invoices and future ones, the way Pr Khaled's sheet has it. Client invoice
  untouched - that stays per garment with no cost on it.
- The two columns already existed. What had never been checked was the
  per-invoice sheet: the Pr Khaled verification above was run against the seven
  **orders**, which take a different path through the builder.

**Rows matched by sticker had no basis at all.** The invoice sheet looked its
fabric line up by `sales_order_line_id` only. Older lines were matched by the
sticker printed on the cut and never had that id written back, so they reached
the sheet with no mill price and no length - INV-2026-0001 entirely, and one
line of INV-2026-0005. It now walks the id, then the sticker, then the article
that sticker names, then a fabric number only when a single line carries it. It
stops before matching on garment type: every Trouser line satisfies that, and a
price from the wrong cloth is worse than a blank cell.

**The length was wrong on every multi-piece garment.** A row here is one billed
article carrying the whole line's fabric cost, but the meters were divided by
piece count. A Shirt+Trouser printed 1.35 m beside the cost of its full 2.7 m,
so the identity in the subtitle reached exactly half. Twenty of eighty-five
priced rows were off, always by their own piece count. Fixed by quoting the
whole line's meters. No cost figure moved. The sitewide sheet divides both cost
and meters and is correct as it stands, so it is left alone; the column header
and subtitle are now per sheet rather than shared.

**Duty is not universal.** Four rows looked wrong by exactly 5% until the rule
surfaced: Canclini and Wool Stock sit in the warehouse with duty long since
paid, so `isImportedFabricSupplier` charges them none. The audit was too strict,
not the sheet. Subtitle now says "5% duty on imported cloth".

- Result across the twelve invoices: **94 of 94** rows carry a length, **85 of
  85** priced rows reconcile to the halala. The nine rows still without a price
  are the nine fabrics no uploaded list covers, blank by design.
- `scripts/audit-cost-hint-basis.ts` measures all of this per invoice.
- Six new tests, and they were confirmed to fail against the old code before
  being trusted.
  Ship: `f8c0bc3b`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## "Blair? Patrick?"

Blair Maxwell was checked, three times over. Patrick Raupach never was, and the
reason matters: **he has no invoice at all.** Every invoice audit in this repo
starts from the invoice list, so a client who was never invoiced is invisible to
all of them. Asking after him found a blind spot, not a bad line.

- `scripts/audit-uninvoiced-orders.ts` starts from the orders instead.
- 133 sales orders, 12 invoiced. 119 live orders carry 1,707 fabric lines with
  no invoice behind them. Almost all are simply still in production.
- **One order is finished and never invoiced**: `SO-2026-0101`, Moiid Al
  Zahrani, 14 lines, fabric cost SAR 5,688.47.
- Patrick's two orders are both open, so no invoice is due yet.

**Patrick's fabric numbers will not resolve when they are.** `25T001`, `25T003`,
`25T232`, `25T142` are real Canclini rows (100% Linen 145g); `25H003` is 206g.
On `SO-2026-0009` the same cloths are written `T001`, `T003`, `T232`, `T142`,
`H003` - the `25` dropped - and none of those resolve to anything. Others carry
free text in the number field: `25T001/Chines Collar Shirt`,
`C1415541-4 (Use for 3 Shirt)`, `10005. 001/204D` with a space,
`CAQW04041-1415541-1`. 14 of 15 lines on `SO-2026-0103` and 17 of 19 on
`SO-2026-0009` have no fabric price. The pairing is the owner's to confirm; the
numbers are not being rewritten on a guess.

**A check that reported zero because it asked the wrong question.**
`resolveFabricItemFromCatalog` never returns null - an unknown number comes back
as a `manual: true` stub with every spec null, which is exactly what makes the
invoice cell blank rather than wrong. Testing for nullishness found 0 unbacked
numbers across 119 orders. Testing `manual` finds **202**, including `Stock`
(72 lines), `TBD`, `Old fabric`, `China old fabric` and `Linen 125` typed into
the fabric number field.

**And a correction to the entry above.** It claimed the type error count was
unchanged. That measurement was taken before the six tests were written, and
those tests added two TS2352 errors by widening their fixtures to
`Record<string, unknown>[]`. Fixed with the `as unknown as` cast already used
elsewhere in the same file; the error list is now byte-identical to the
pre-session baseline at 262.
  Ship: `0dec3f47`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Drapers images, every bunch its own folder

- Owner: are we connected to Drapers, did we already download all their fabrics
  at highest resolution, and if not do it with each bunch in its own folder.
- Connected, yes: `api.drapersitaly.it`, key `DRAPERS_API_KEY` passed as `ak`.
  **This VM has no key and no `.env.local`**, so nothing can be downloaded from
  here - and nothing here can write to the owner's Mac either way.
- Already done, twice. `data/suppliers/drapers/images-by-collection/manifest.json`
  records a `best`-quality per-collection run: **1,527 of 1,532**, 30 folders.
  The flat `images/manifest.json` is a later run, same 1,527. The JPEGs are
  gitignored, so only the manifests are in the repo.
- Resolution is genuinely per-fabric best: 1,030 at 1500x1500, 146 at 2071,
  **69 at 4000x4000**, 489 at 2000px or larger. `--by-collection` already forces
  `--best`, so the two cannot be asked for separately.
- The five that never resolve are `12004`, `12009` (THREE KINGS), `17037`
  (LIGHT PANAMA), `19030` (SUPERSONIC), `82110` (OVERCOAT) - all "Product not
  found" at the API.

**One bunch was landing in two folders.** The slug turned `&` into nothing but
kept the word `and`, so `AI25 - SUPERBIO & BEAUSOLEIL` (79) and `Superbio and
Beausoleil` (3) split, as did `AI20 - EARTH WIND & FIRE` (10) and `EARTH WIND
AND FIRE` (10). Folding the joining word gives **31 folders, none split, none
uncategorized**. Left alone deliberately: `golden-selection` vs
`golden-selection-ss-2026`, and `lady-sanfelice` vs `lady-sanfelice-flannels` -
those are different names, not two spellings, and merging them would be a guess.

**A failed run was erasing the record of a successful one.** The images are
gitignored, so the manifest is the only tracked memory of what was downloaded.
A missing API key fails every fabric and then writes that over the top - running
the script here with no key replaced 1,527 good entries with failures, and it
had to be restored from git. It now refuses to overwrite a manifest holding
successes with a run that saved nothing.

- New `--plan` prints the folder plan, the expected size (~1.1 GB from the last
  run's recorded bytes) and the API call count, without downloading or needing
  a key.
  Ship: `76fd412e`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Where the Drapers images already live, and resuming a fresh download

Ralph recalled having downloaded them onto the ERP already. He was right, and it
went further than the earlier note here said: they were not only fetched, they
were pushed into the ERP's own storage in July.

- `a72ee109` (26 Jul) cached specs and swatches; `773a6c6c` (26 Jul) uploaded to
  Supabase; `3fe594db` (27 Jul) hardened the sync with missing-only uploads.
- `src/data/suppliers/drapers-swatch-index.json`, written `2026-07-26`, carries
  **1,527 fabrics** - exactly the ones that downloaded cleanly. The five Drapers
  never served (`12004`, `12009`, `17037`, `19030`, `82110`) are absent from it,
  so those are the only Drapers cloths that can come up blank in the app.
- **1,485 of those 1,527 carry a filename and no remote URL.** For them the
  `erp-fabric-swatch` bucket is the sole source; there is no falling back to
  drapersitaly.it. Only 42 have a remote URL as backup. So a Drapers swatch
  rendering in the live app is itself the proof the bucket is populated.
- The bucket is private and this VM has no service-role key, so the live object
  count cannot be read from here. On the Mac,
  `node scripts/sync-drapers-swatches-to-supabase.mjs --missing-only --dry-run`
  prints `Found N existing object(s) under drapers/` and uploads nothing.
- The bunch folders are a Mac-side concern only. Storage is deliberately flat
  (`drapers/10101.jpg`, keyed on fabric number), so yesterday's folder fix
  requires no re-upload.

**A full download could not survive being interrupted.** 1,532 fabrics is about
6,100 HTTP calls and 1.1 GB; the manifest was only written after the last one,
so Ctrl-C or a dropped connection threw away the record of everything already
fetched and the next run re-downloaded all of it.

- New `--resume` skips any fabric whose image is already in the output folder,
  checked against all four extensions since which one it lands under is only
  known after the API call. Skipped entries keep their recorded quality and
  dimensions from the previous manifest, so a resumed run's manifest is as
  informative as an uninterrupted one.
- `SIGINT`/`SIGTERM` now write the manifest before exiting, marked
  `interrupted: true`, and print the command to carry on with. The guard against
  overwriting a good manifest with a worthless one applies on that path too -
  verified by interrupting a keyless run against a folder holding a manifest
  with successes, which left it byte-identical.
  Ship: `b03c2f3f`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Retiring the named-client cost hint downloads

The offline pricing pack Ralph asked for by name is downloaded, so the two
buttons that produced it are gone from both the invoicing and the costing page:
"Download Ibrahim, Mitwalli, Hicham, Al Sheikh, Pr Khaled" and "Download Pr
Khaled missing fabric prices". The costing panel's blurb about Pr Khaled's seven
orders went with them, since it described a button that no longer exists.

What stays is the general machinery: `?clients=` and `?missing=1` are still
parsed by both hint-pdf routes and still filter the worksheet, so either pack can
be pulled by URL if it is ever wanted again. Only the buttons were one-off, not
the capability behind them. `cost-hint-clients.ts` is untouched.

Type errors 262, unchanged from baseline. Costing tests 35/35.
  Ship: `b4dafa55`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Quoted prices reaching every cut on Abdelaziz Al Ajlan's hint

Ralph asked for the prices he quoted on INV-2026-0007 to show on the cost hint.
Most already did - `sellingPriceByFabricLineId` maps the invoice's unit price onto
the order line it was raised against - but 8 of 30 rows on SO-2026-0119 printed a
blank selling price beside a real cost.

The cause is consolidation. An invoice article can bill several cuts (six
trousers of one fibre and weight as one row), and only the first keeps a
`sales_order_line_id`. The rest looked unbilled. Worse, the matched row claimed
the whole invoiced quantity while its siblings still counted themselves, so the
sheet showed 11 trousers where 6 were cut, 7 shirts where 4 were cut.

`spreadConsolidatedSellingPrices` now hands the price to the leftover cuts,
keyed on garment + fibre + weight inside one sales order.

- **The mill is deliberately not in the key.** The sales order files every
  Solbiati cloth under the `loro-piana` supplier id while the invoice prints
  "Solbiati" beside it. Including the mill would fail on precisely the
  consolidated lines this exists to match (S23009, S10005, S05007).
- **Quantity is moved, not invented.** Whatever goes to the siblings comes off
  the matched row, so the total is conserved and nothing can be conjured that
  was not billed. A cut the invoice has no quantity left for keeps its blank.
- Once priced, the rows merge on the existing group key, which is what removes
  the double count - the fix to the price is the fix to the quantity.

SO-2026-0119 now reads **39 garments cut, 35 priced, 4 blank** against an invoice
of 35, and the 4 blanks are exactly the unbilled ones: S23036 Overshirt, 781060
and 781056 Shirt LS, and the 772003 Short that article 12 mislabels. Sitewide the
only change is this order: 12 phantom garments gone (blank quantity 606 -> 594),
priced quantity unchanged at 132.

Tests 1145, 6 pre-existing failures. Type errors 262, unchanged.
  Ship: `9d968ea5`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Filling a hint price from the cloth and garment on the invoice

Ralph: "from the compositions and weight on the invoice and brand you can know
how to fill on the hint." He prices by cloth and garment, so two Shirt LS in the
same AUSTRALIS 250g are worth the same whichever number is on the bolt. A cut the
invoice never billed still has a knowable rate.

`inheritedSellingPriceByFabricLineId` copies it, matched on garment + fibre +
weight within one sales order. The mill stays out of the key for the same reason
as the billed match.

Two guards, because a rate is not a receipt:

- An inherited row keeps a **blank invoice number**, and `costHintInvoiceGroupKey`
  now includes the invoice, so it cannot merge into a billed row at the same
  figure. Without that, 781056/781060 at 3,800 would have folded into 781059's
  billed row and a garment made but never charged for would have vanished behind
  an invoice number. The PDF already prints an INV column whenever it is not
  uniform, so these rows show a price against "-".
- Where one cloth and garment carry **two different prices** on the invoice there
  is no single rate, so nothing is filled.

On SO-2026-0119: S23036 Overshirt takes 4,500 from S23037, and 781056/781060 take
3,800 from 781059, all three with no invoice number. **772003 Short stays blank** -
it was only ever billed inside a combined article (the mislabelled article 12),
so no Short price in that cloth exists to copy. That is the right answer: it is
the one line where Ralph still has to decide something.

Sitewide: 132 garments billed, 3 carrying an inherited rate, 591 blank (orders
with no invoice at all, so nothing to inherit from).

Tests 1149, 6 pre-existing failures. Type errors 262, unchanged.
  Ship: `b8b21570`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
## A suggested price, in its own column, drawn from every invoice

Ralph: analyse the fabrics, weight, composition and garment against the pricing
he did, find where the same cloth and garment were priced alike, and suggest a
quote for the Al Ajlan clients on each of their hint pages.

Checked first, because the suggestion is only worth making if his pricing is
consistent. Across 12 invoices there are **54 garment + fibre + weight
combinations, and 52 carry exactly one price** - the same figure whichever
fabric number was on the bolt and whichever client bought it. The two that do
not are the reason for the conflict rule: a Shirt LS in 71/15/14 at 250g went
out at both 3,100 and 3,300, and an Overshirt+Trouser in the same cloth at both
7,500 and 7,800. Choosing between two prices he set is his call, so those two
combinations are dropped rather than resolved.

That changes the inherited rate shipped in `b8b21570` in two ways.

- **It is sourced from every invoice, not just the one on the same sales order.**
  `buildCostHintPriceBook` reads all of them, so a cloth priced once for one
  client can suggest for another. Reach goes from 3 garments to 71 rows across
  34 rates.
- **It moved out of the unit price column.** `suggested_price_sar` is its own
  column with `suggested_price_basis` beside it naming the cut and invoice the
  figure came from, so the suggestion can be argued with. The unit price column
  now holds only money someone was actually charged. Cuts the invoice did bill
  are skipped, so a suggestion can never contradict a real price.

The column and its note appear only when something can be suggested, which keeps
the per-invoice sheet unchanged - it has no order lines behind it to suggest for.

Six client codes carry an Ajlan name, so the three he asked for are pinned to
codes here. `FR-0726-0038` "Abdelaziz Ajlan Al Ajlan" and `FR-0726-0039`
"Abdelaziz Mohamad Al Ajlan" are different men.

- **Abdelaziz Ajlan Al Ajlan** (`FR-0726-0038`). SO-2026-0125 is fully billed on
  INV-2026-0009. **SO-2026-0118 is billed nowhere**: 9 Overshirt+Trouser and 2
  Shirt+Short, all 11 suggesting SAR 77,300 off INV-2026-0009's own rates. This
  is a question, not money found - see below.
- **Abdel Aziz Fahd Al Ajlan** (`FR-0426-0006`). SO-2026-0129 fully billed on
  INV-2026-0012. SO-2026-0008 is 14 of 22 billed and the other **8 cannot be
  suggested for**: three cashmere Overcoats at 480g and 550g, two Shirt LS whose
  composition reads `80-2,100-1-78%Cotton18%Polymide4%Elastane` (a yarn count
  glued to a misspelt Polyamide), and three Caccioppoli Shirt LS carrying no
  weight at all. The only Overcoat ever invoiced went out at 16,500 on this same
  client's INV-2026-0004, but that cloth (`505050`) has no weight recorded, so
  the sheet will not copy a figure across an unknown.
- **Ajlan Mohamad Al Ajlan** (`FR-0626-0035`). Nothing to suggest: every cut on
  SO-2026-0107 and SO-2026-0122 is already on an invoice.

**SO-2026-0118 is probably a superseded draft of SO-2026-0125**, not a second
batch: same client, two weeks earlier, largely the same cloths, but longer cut
lengths (3.9 m against 3.0 m for the Overshirt+Trouser, 3.5 m against 2.4 m for
the Shirt+Short). 0118 has S14022 which 0125 lacks; 0125 adds five T-shirts and
a Trouser. Neither is archived, so both print. Ralph's call.

Tests 1151, 6 pre-existing failures. Type errors 262, unchanged.
  Ship: `4732cfbb`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
