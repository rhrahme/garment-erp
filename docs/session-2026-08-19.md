# Session 2026-08-19

Production: https://erp.hagan.pro - branch `main`

## Mem0 extra brain (Cursor MCP)

- Owner asked for a third-party brain layer for everything we build.
- Wired Mem0 MCP (`https://mcp.mem0.ai/mcp/`) in local Cursor mcp.json.
  Account signup is still pending (no Mem0 account yet).
- Local rule `.cursor/rules/mem0-owner-brain.mdc`: save `Owner:` (user
  words) and `Agent:` (what we shipped). Owner wins if they conflict.
  KNOWLEDGE.md stays the shipped source of truth.


## Live leftover after approved Kashif deletes + 10 PM rule

- Owner pointed at Stitch kiosk Live 1: Kashif FR-0134-L05-SHT-1/2
  (771011) still open at 00:55 with 13h elapsed. That delete was already
  approved at 00:41; a later sequential write put the session back
  (protect-merge only skipped remote re-add, not a stale incoming row).
- Removed that leftover from live immediately. Protect-merge now keeps
  `deleted_session_ids` tombstones and will not reopen a remote-closed
  session if a kiosk flush still has it open.
- Owner: leftover Live rows should stop at 10 PM Riyadh either way. The
  Aug 18 workday cap only trimmed hours when someone later closed. Now
  forgotten open/closing sessions auto-close at 22:00 (cron + Live poll).
  Overtime scans after 22:00 stay logged and queue `overtime_confirm`
  for admin. Confirm counts Performance; Reject keeps History but drops
  hours.

## Pattern logins checked before overnight

- Both pattern accounts are live and unlocked. Mohtajul password hash
  still matches; Email tab `hagan.dp1@gmail.com` or Badge `2625917972`.
  Pattern 2: Badge `XX22` + the password already set. If erp.hagan.pro
  times out on the factory PC, use https://garment-erp-kvsf.vercel.app/login.



## Admin emails: one-click accept for stitch + fabric delete requests

- Owner: email admin so he can accept the requests. Client name emails
  already had Approve/Reject links; stitch and fabric-delete emails only
  pointed at the dashboard (and could fall back to localhost).
- Fix: those emails now include per-admin signed Approve/Reject links
  plus the /approvals page (7 days, no login). GET
  /api/admin-approvals/email-action is session-exempt like the name-change
  email action.

## Dashboard hid stitch kiosk requests

- Four pending stitch@ delete requests (Kashif, FR-0134/0135) were live
  but the dashboard never loaded `sewing_session_change_requests`, so the
  admin card stayed empty.
- Dashboard now fresh-loads that store, shows a Stitch kiosk requests
  card, and the queue panel no longer depends on a cold cache.

## Pattern desktop login: keep the email path he knows

- Ralph signed in with badge 2625917972 from his Mac - credentials work.
  Mohtajul's factory desktop still cannot: Chrome reinstall / cache clear
  cannot fix the earlier ERR_CONNECTION_TIMED_OUT, and he has used the
  Email tab for months.
- Login page opens on Email again. Email or Badge accepts his old
  mailbox password or the badge password, then starts the badge session.

## Admin login log (who / time / device / IP)

- Owner asked if we show which IP tried to log in. We did not - only
  badge last_login_at. Added `/logins` (admin sidebar) plus a dashboard
  Recent logins strip. Each email/badge success or failure stores actor,
  identifier, IP (x-forwarded-for), device label, and raw user-agent.
  Zapier: auth.login / auth.login_failed + GET /api/v1/auth/logins.
