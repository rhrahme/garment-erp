# Session 2026-08-19

Production: https://erp.hagan.pro - branch `main`

## Admin can copy ERP text

- Owner: as admin he could not copy any text from the ERP. Unlock copy for
  admin only.
- Shipped: admin session adds `admin-copy-unlock` so text is selectable.
  Stitch scan capture no longer steals focus or clears selection for admin.
  Floor dashboard names/badges are real text (not a full-row button).

## Admin: pick a stitcher and see day / week / month

- Owner: on the stitch kiosk, admin should select an employee and see
  daily, weekly, and monthly work. Also see who of the employees did not
  scan anything yet, as a proper dashboard.
- Shipped: Performance tab (admin login only) is a Floor dashboard -
  Expected / Scanned / Didn't scan / Live / Pieces, then the missing
  list for investigation. Tap a name for today / week / month pieces.
  Roster is active Expats with a floor job. Rejected overtime still
  counts as present; it stays out of hours.

## Pattern How-to tab + notify them on every floor fix

- Owner: when we explain a fix to Pattern (first: remove one fabric from a
  consolidated group), send them a notification - not only chat. Also add
  a tab of every explanation so new employees can read it later.
- Shipped: Pattern workspace tabs Queue / How-to / Library. Catalog
  `PATTERN_HOWTO_NOTICES` emails `PATTERN_EMAILS` and keeps the steps on
  `/pattern/how-to` after Got it. New entry:
  `howto-remove-fabric-from-consolidation-v1` (Grouped fabrics -> Remove).
- Agent rule: `.cursor/rules/floor-howto-notify.mdc` - add a catalog
  entry whenever we explain a floor fix.

## Mem0 extra brain (Cursor MCP)

- Owner asked for a third-party brain layer for everything we build.
- Wired Mem0 MCP (`https://mcp.mem0.ai/mcp/`) in local Cursor mcp.json.
  Owner supplied a Mem0 API key. Key lives only in local Cursor/shell
  config, not the repo. First Owner/Agent memories saved (lunch clocks,
  Pr Khaled short name, Live empty at 22:00). Reload Cursor MCP so the
  mem0 tools connect in chat.
- Local rule `.cursor/rules/mem0-owner-brain.mdc`: save `Owner:` (user
  words) and `Agent:` (what we shipped). Owner wins if they conflict.
  KNOWLEDGE.md stays the shipped source of truth.


## Live leftover after approved Kashif deletes + 10 PM rule

- Owner pointed at Stitch kiosk Live 1: Kashif FR-0134-L05-SHT-1/2
  (771011) still open at 00:55 with 13h elapsed. That delete was already
  approved at 00:41; a later sequential write put the session back
  (protect-merge only skipped remote re-add, not a stale incoming row).
- Removed that leftover from live immediately. Protect-merge now keeps
  `deleted_session_ids` tombstones and will not reopen a remote-closed
  session if a kiosk flush still has it open.
- Owner: leftover Live rows should stop at 10 PM Riyadh either way. The
  Aug 18 workday cap only trimmed hours when someone later closed. Now
  forgotten open/closing sessions auto-close at 22:00 (cron + Live poll).
  Overtime scans after 22:00 stay logged and queue `overtime_confirm`
  for admin. Confirm counts Performance; Reject keeps History but drops
  hours.

## Pattern logins checked before overnight

- Both pattern accounts are live and unlocked. Mohtajul password hash
  still matches; Email tab `hagan.dp1@gmail.com` or Badge `2625917972`.
  Pattern 2: Badge `XX22` + the password already set. If erp.hagan.pro
  times out on the factory PC, use https://garment-erp-kvsf.vercel.app/login.



## Admin emails: one-click accept for stitch + fabric delete requests

- Owner: email admin so he can accept the requests. Client name emails
  already had Approve/Reject links; stitch and fabric-delete emails only
  pointed at the dashboard (and could fall back to localhost).
- Fix: those emails now include per-admin signed Approve/Reject links
  plus the /approvals page (7 days, no login). GET
  /api/admin-approvals/email-action is session-exempt like the name-change
  email action.

## Dashboard hid stitch kiosk requests

- Four pending stitch@ delete requests (Kashif, FR-0134/0135) were live
  but the dashboard never loaded `sewing_session_change_requests`, so the
  admin card stayed empty.
- Dashboard now fresh-loads that store, shows a Stitch kiosk requests
  card, and the queue panel no longer depends on a cold cache.

## Pattern desktop login: keep the email path he knows

- Ralph signed in with badge 2625917972 from his Mac - credentials work.
  Mohtajul's factory desktop still cannot: Chrome reinstall / cache clear
  cannot fix the earlier ERR_CONNECTION_TIMED_OUT, and he has used the
  Email tab for months.
- Login page opens on Email again. Email or Badge accepts his old
  mailbox password or the badge password, then starts the badge session.

## Admin login log (who / time / device / IP)

- Owner asked if we show which IP tried to log in. We did not - only
  badge last_login_at. Added `/logins` (admin sidebar) plus a dashboard
  Recent logins strip. Each email/badge success or failure stores actor,
  identifier, IP (x-forwarded-for), device label, and raw user-agent.
  Zapier: auth.login / auth.login_failed + GET /api/v1/auth/logins.

## Client short name: Pr Khaled, not Pr Salman

- Owner: Live / lists were showing Pr Salman for Pr Khaled Bin Salman.
  Short labels must show Pr Khaled sitewide.
- Shipped: `formatClientShortName` keeps first + last for ordinary names
  (Abdel Ajlan). With a title, short label is title + first name.
- Owner: add a Title dropdown before First (Mr, Mrs, Dr, Pr, Sheikh, ...).
  `title` is its own field. Legacy "Pr" in first_name is lifted to
  title=Pr, first=Khaled. Clients page + inline create use the dropdown.
  Zapier POST /api/v1/clients accepts title.

## Lunch 14:00-16:00 Riyadh must stop Live clocks

- Owner: lunch is 2-4 PM Riyadh. Live elapsed was still running at 15:05
  because time only froze if someone tapped Pause.
- Fix: every session subtracts 14:00-16:00 Riyadh automatically (no pause
  tap required). Scan gate auto-pauses at 14:00 and auto-resumes at 16:00.
  Crons 11:00 UTC / 13:00 UTC. Owner sentence wins over "admin paused".
- Follow-up: first ship rewound Live `now` to 14:00, so the Lunch segment
  disappeared and clocks still looked like running work time. Keep wall now,
  subtract 14:00-16:00, show Frozen for lunch.

## Ready-made garment and size photos

- Owner: on Ready-Made, each garment and each size must accept image
  uploads.
- Added Photos on each article. Garment-level shots plus XS-XXL (add extra
  sizes). Catalog in `ready_made_catalog`; Zapier GET/POST catalog + image
  upload.

## ID badge jobs: Champa and Washing

- Owner: for the jobs in ID, add Champa / Button hole and button fixing,
  and washing.
- Catalog now has `champa` (Champa / Button hole and button fixing) and
  `washing` (Washing). They sit next to the existing `buttons` and
  `wash_iron` jobs; Cherry's IRONING/BUTTONS dual-QR is unchanged.
  Live/Scan use those labels instead of Sewing. Floor dashboard Missing
  includes people assigned only those roles. Assign on `/hr/id-badges`.
- Ship: `9224be9`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Sitewide fabric and garment photo uploads

- Owner: every garment, every fabric, anything, anywhere - they should
  be able to upload images. Sitewide.
- One album store (`entity_images`) plus a reusable Add strip on orders,
  receiving, pattern, invoices, fabric spec, production, and stitch.
  Fabric photos follow the cloth; garment photos follow the type;
  article photos stay on that SO line. Zapier GET/POST
  `/api/v1/entity-images`. No upload buttons on print sheets.

## INV-2026-0009 composition missing mill codes

- Owner: why does composition not start with the fabric brand - LP, SOLB,
  DP, ZE, Cacci.
- The invoice already formats `{brand} {fibre} {weight}`, but the mill
  list only had LP / DP / CC / SB. Solbiati, Zegna, and Caccioppoli on
  INV-2026-0009 therefore printed as fibre only. Added SOLB, ZE, Cacci
  (and hyphenated ids like loro-piana). Screen, print, and PDF pick it up.
- Follow-up: owner does not want mill collection names on the invoice.
  PEGASO DELAVE 100%LINEN 340g becomes SOLB 100% Linen 340g. Same for
  quoted styles (SUMMERTIME) and Cacci tela / yarn notes.
- Owner: keep KNIT on those Loro Piana T-shirts, sitewide, same logic on
  every invoice. "KNIT SHIRT" / KNIT stays as Knit; Piquet and Honey Comb
  still drop. L12/L13 print LP 100% Cotton Knit 160g.

## Stitch kiosk fabric color preview

- Owner: in the stitch kiosk, add the fabric color preview so we can see
  it if needed.
- Scan / Live / History / Orders show the mill swatch (tap to enlarge).
  New sessions store `supplier_id`; older rows backfill from the SO line.
  stitch@ is allowed the fabric swatch image APIs.
- Ship: `ea44cd4` (fix after `9352250` pulled stage-scan into the kiosk
  client bundle). Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Rohan badge is wash / iron, not sewing

- Owner: Rohan badge 2625918129 is washing and ironing -- Sewing /
  Alteration QRs do not make sense.
- Wash / iron people (wash_iron and/or washing, no tailor) now print
  WASHING (`EMPWASH`) + IRONING (`EMPIRON`). Cherry stays IRONING +
  BUTTONS. Reprint Rohan's card.

## Niraj badge is buttons, not sewing

- Owner: Niraj is doing buttons -- same problem as Rohan (Sewing /
  Alteration on the card does not make sense).
- Buttons-only people (buttons, no tailor, not Cherry's pair -- Niraj
  `2625917592`, Junaid `0024`) now print BUTTONS (`EMPBTN`) on both
  squares. Cards always have two QRs; both start a Buttons session.
  Shahryar / Cherry (`2543411918`) stays IRONING + BUTTONS. Reprint
  Rohan, Niraj, Junaid, and Shahryar (if his plastic is still
  Sewing/Alteration).
- Ship: `f8d6870`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
