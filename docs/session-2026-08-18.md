# Session 2026-08-18

## Failed scans explained (ambiguous_shared_piece)

- Owner asked why 3 kiosk scans failed (History > Failed scans). All three
  were the designed shared-piece guard: two stitchers had open sessions on
  the same piece (FR-0133-L35-OS: Kabir Hussain + Sameer Nadeem;
  FR-0133-L14-TR-2/2: Abdul Hafeez + Asif Ali) and someone scanned the bare
  A4, so the kiosk could not tell whose session to close. In every case the
  stitchers then badged first and closed correctly. No stuck sessions.

## Workday cap: kiosk close never records time past 22:00 Riyadh

- Owner: the floor finishes at 10 PM. Forgotten overnight sessions were
  closing at next-day scan time and recording 13-97h elapsed.
- Code: capSessionCloseAtWorkdayEnd() in stitch-kiosk-lunch.ts - kiosk
  closes cap ended_at at 22:00 Riyadh of the session's start day; sessions
  started at/after 22:00 cap at the next day's 22:00 (never zeroed).
  Applied in closeSessionWithBadgeOrPiece after the pending-stop-request
  check (a valid stop request time still wins). Tests added (7/7 pass in
  stitch-kiosk-lunch.test.ts).
- Data: bulk-corrected 30 closed sessions in Supabase (21 spanned
  overnight at 13-97h, e.g. Abdulla Qamar / Ali Raza FR-0133 OS 97.4h ->
  11.2h, Rone Astar L33 40.9h -> 2.7h; plus small trims for closes a few
  minutes past 22:00). Asif Ali FR-0133-L14-TR-2/2 corrected 25h -> 10.8h
  (ends Sat 16 Aug 22:00).
- Known ambiguity left as-is: Parvaiz Ahmad FR-0132-L10-TR-2/2 STARTED
  22:02 Riyadh and closed next afternoon (15.1h kept - no way to infer the
  real stop; owner can supply a time to correct).

## Client ready-made samples (all teams, badge-scan receive)

- Owner: clients sometimes hand us their own garment as a reference sample.
  Every team must be able to record it (brand, color, size, notes) and
  upload photos; the person physically receiving it must scan their
  employee ID badge; and there is a standing reminder to give it back.
- Data: ClientProfile.ready_made_samples (types in clients.ts). Bulk
  PUT /api/clients always carries samples over - they only mutate via the
  dedicated API. Receiver resolves via findPayrollEmployeeByBadgeValue
  (badge QR or ID number); POST rejects without a recognized badge.
- API: /api/client-samples (GET list, POST create), /[id] (PATCH incl.
  returned:true/false, DELETE incl. storage cleanup), /upload-url +
  /register (signed direct-to-storage, same pattern as client photos,
  bucket erp-client-photos subdir client-samples/), /upload (multipart
  fallback), /[id]/images/[imageId] (GET serve, DELETE). v1 parity:
  /api/v1/clients/ready-made-samples (GET/POST, received_by_name allowed
  for API callers). Events: client.ready_made_sample_added / _updated /
  _returned / _deleted (+ ZapierSetup list).
- UI: ClientReadyMadeSamplesPanel on every client card (/clients) - add
  form with badge scan box, photo grid, amber "Give it back to the
  client" pill until Mark returned (undo supported).
- Permissions: /api/client-samples allowlisted for ALL restricted roles;
  stitch + task operators additionally got /clients page + nav (stitch
  also /api/clients for the editor's list fetch). Verified end-to-end as
  stitch@hagan.pro: page 200, badge required (400 without), create,
  signed + multipart upload, image serve, mark returned, delete.

## Marketing tab: Suits (Young) lookbook

- Owner uploaded Suits_Young.pdf (3 suit proposals, plain text tables) and
  asked for high-res fabric images, a nicer layout with explanation and
  sketch, under a new Marketing tab.
- New /marketing dashboard tab (sidebar Megaphone icon; admin + sales -
  sales presents lookbooks to clients) listing the Suits (Young) document;
  /marketing/suits-young is the print view (A4 recipe: pt fonts,
  Helvetica, one suit per page + cover, page-break rules).
- Content lives in src/lib/marketing/suits-young.ts with real catalog
  specs (Loro Piana 779053 Uniti merino stretch; 707080 + 707038
  Australis Super 150's, 250 g/m2, 150 cm). High-res swatches (1200x1800)
  resolve via /api/suppliers/loro-piana/images/... for 707080 + 707038.
  779053 and the Skyline/Oliani alternatives have NO digital swatch
  anywhere (bucket + local checked) - they render styled "swatch photo
  pending" tiles until photographed.
- OutfitSketch.tsx: SVG fashion flat (jacket/trousers in suit fabric with
  stripe/dot patterns, shirt + tie in the V, optional shoes) - one sketch
  per pairing on the lookbook.
- 779053 follow-up (owner asked why it was not showing): the article is
  real in the SS26 catalog (UNITI, 250 g/m2 Zelander stretch merino) but
  the "Uniti 779 Instakit" download only contains per-article photos of
  779010/779042/779055 plus ONE group drape shot (_90A7640.jpg). 779053
  was never photographed by Loro Piana in that kit. Fix: cropped the blue
  drape from the group shot to public/marketing/uniti-779-blue-drape.jpg
  and show it on the Blue suit page with an italic caption "Bunch photo
  (Uniti 779) - exact swatch of N 779053 to follow" (new optional
  image_note on LookbookFabric + .lb-swatch-note style). Replace with the
  real swatch when the bunch page is photographed.

## Stitch floor Performance + History: garment type filters

- Owner: "performance and history need better organisation... add filters
  about type of garment".
- New garment chip bar (StitchFloorWorkspace) under the Daily/Weekly/
  Monthly selector, shown on Performance and History>Sessions (hidden on
  Failed scans - failures carry no garment type). Chips use the floor
  color legend (garmentTypeLegendEntry) with per-garment session counts;
  All resets. Filter resets when the period changes.
- Keys are normalized via normalizeGarmentTypeColorKey on the session's
  piece article (so the Trouser piece of an Overshirt+Trouser order
  counts as Trouser, not the set).
- Performance: server aggregate when All; with a filter active the
  aggregates (pieces/total/avg) are recomputed client-side from closed
  sessions and re-ranked. Header shows "X only: N pieces, T total".
  The employee card's plain "Overshirt / Trouser" text was replaced with
  colored chips + counts (Overshirt x24, Trouser x17, ...), and the
  expanded piece list honors the filter.
- History: rows filter by garment before search/sort; header shows a
  colored "Overshirt - 76" pill with the filtered count.
- Verified in browser (Monthly, 255 sessions): chip counts sum correctly,
  Overshirt filter re-ranks employees (76 pieces across 12 stitchers),
  History pill + table match, Failed scans unaffected.
