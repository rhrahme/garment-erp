# Session 2026-07-03

Production: https://erp.hagan.pro · branch `main`

## Highlights

### Orders & data fixes
- **Patrick Raupach (GL-0326-0003 / SO-2026-0103):** Restored 15→18 lines; added suit trouser lines + JKT+TR labels (`aa776f4`).
- **Blair Maxwell (SO-2026-0005 / INV-2026-0006):** Invoice prices not recoverable after restore wiped `unit_price`; re-enter on INV-2026-0006 and click **Save line prices** (no autosave). Restore scripts now preserve prices (`2e7d545`).

### Supplier emails
- **Solbiati + Loro Piana:** One combined PO/email to factory inbox (`926b945`).
- **Sent state:** Fixed lost `emailed_at` race on send (`81efe8b`).
- **Partial send:** Checkboxes per fabric line; unselected stay pending (`7080c66`).

### Fabric Receiving
- Fabric sticker paste first (e.g. `FR-0226-0024/ 0109-L32`); employee badge optional under Floor scanner (`e429b07`, `f45db67`, `7dab584`).

### Invoicing & documents
- **Riyadh bank PDF:** Documents & Data + Invoicing download; generates on Vercel (`2484d9c`, `0b6348f`, `9babf9b`).
- **Submitted timestamp** on fabric order requests (`9c989a6`).

### Sales / fabric orders UI
- **Column sorting** on fabric lines (`9007131`).
- **Total fabric cost** for admins — supplier catalog fallback (`1c69e22` + catalog lookup fix).

### Deploy
- Vercel build fixes: `writeJsonFileAsync` export, client-safe reference constants (`5e8a7fb`).

## Key commits (newest first)

| SHA | Summary |
|-----|---------|
| `1c69e22` | Total fabric cost on SO / fabric order (admin) |
| `7080c66` | Partial supplier email line selection |
| `9007131` | Sortable fabric line columns |
| `9c989a6` | Submitted-by date/time on fabric orders |
| `81efe8b` | Fix fabric order sent state after email |
| `926b945` | Combine Solbiati + Loro Piana emails |
| `aa776f4` | Patrick Raupach order restore |

## Reminders

- Invoice line prices: click **Save line prices** — preview updates live but does not persist until saved.
- Supplier email partial send: emailed lines drop off the picker; batch moves to Sent when all lines are sent.
