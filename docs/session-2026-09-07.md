# Session 2026-09-07

Production: https://erp.hagan.pro - branch `main`

## All-teams how-to on every ERP account

- Owner: send all teams notifications and highlights, English and Bangla
  always, on their ERP accounts also (not email only).
- How-to `howto-sent-stitched-garment-v2` emails every team mailbox.
  Amber banner on every dashboard page until that login taps Got it.
  How-to is on the left menu (`/how-to`) so it stays after Got it.
  Pattern still has Pattern -> How-to for Pattern-only cards.
- Ship: `9e2243e`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Client garments photo button

- Owner: where is the button to upload images of the client's garment?
- Owner: call the block Client sample so the team understands it.
  Open the client, scroll to Client sample, press Add garment, then Add
  photos. Was Client ready-made samples / Client garments.
  Saved cards also have Add photos. How-to body now names that button.
- Owner: ship and send them the explanation again. New id
  `howto-client-sample-garment-v3` (v2 already had emailed_at, so a body
  edit would not re-email). All-teams email + amber banner + How-to tab.
- Ship: `83594fe`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
  Email `emailed_at` 2026-09-06T17:26:10Z.

## Boggi Overcoat house base

- Owner: ship the Boggi update. Pattern Library Bases already has Boggi
  / Overcoat on FR and GL (sizes 44-68, file Boggi Measurement Spec for
  Overcoat.xlsx). Jacket and Trouser stay on FR. Promotion helpers
  shipped so the folder can be rebuilt without committing the JSON dump.
  How-to `howto-boggi-brand-folder-v1` is already on Pattern.
- Ship: `3bbb433`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Copy a house pattern to another brand

- Owner: send Pattern how to copy patterns and edit them.
- Open a base, pick the other brand, Copy to brand, edit numbers, Save
  changes. Same cut already there opens the existing sheet. For a real
  client: Load from base pattern, then change his sizes.
  How-to `howto-copy-base-to-brand-v1` emails both Pattern mailboxes.
- Ship: `f30dd73`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## QC badge login (Mahmudul / 2587734852)

- Owner: set a badge password for ID 2587734852 (Mahmudul Hassan).
  He is the new QC person. Same locked QC surface as Hossein
  (`hagan.qc@gmail.com`): Production Orders, Quality, Clients,
  Client sample, fabric receiving/orders/spec, thread & buttons,
  Production, Stitch kiosk (Correct start time), Expats ID badges,
  How-to. Prices and accounting stay sealed (no invoices, costing,
  purchasing, supplier invoices, payroll, Pattern library, Inventory).
  `qc` wins over `pattern` in job_functions. Session
  `badge-qc-<id>@badge.hagan.pro` / client_manager. Landing `/orders`.
  Do not store the password in how-to, git, or Mem0.
- Ship: `d37761b`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
  Production credential written for badge 2587734852 as kind `qc`.

## Morning HERE wall attendance

- Owner: for attendance, print several wall QRs; they scan the poster
  and their ID. Either order. Marks them here. Does not start or stop
  a piece. Admin Floor dashboard shows Here when 0 pcs. Print
  `/stitch/attendance/print`. How-to `howto-here-wall-attendance-v1`
  emails every team EN+BN + banner + How-to tab.
- Ship: `9172af7`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
  Email `emailed_at` 2026-09-06T19:05:24Z.
- Floor dashboard roster crashed on first load (employee list helper
  was re-exported, not imported). Fix: `80268b0`. Both Vercel Ready
  (`garment-erp`, `garment-erp-kvsf`).

## Wall Attendance QR today, clock-in tomorrow

- Owner: print the wall QR today; attendance starts tomorrow. Do **not**
  ask anyone to double-scan a garment A4. Pair is **personal ID badge,
  then the wall QR** (`ATTEND`). Hung `HAGAN-HERE` posters still work.
- Today (7 Sep 2026 Riyadh): print and hang. They may scan today to
  test. That write is ignored on the roster. Tomorrow the time they
  scan is the official clock-in.
- From 8 Sep 2026 00:00 Riyadh: badge then wall QR writes
  `stitch_attendance`. Stay armed for the first A4. No sewing session,
  no admin start email, no performance hours. No badge: scan the badge
  first. Already signed in: one mark per person per Riyadh day. Wall QR
  never closes an open piece.
- Floor scanned = clock-in on that day (go-live+) or a session that
  touches the day.
- Print: Floor now / Stitch Performance / `/stitch/attendance/print`
  and `GET /api/production/attendance-qr`. Events
  `production.attendance_checked_in` + `production.attendance_clocked_in`
  (employee, kiosk, time; no prices).
- How-to `howto-wall-attendance-qr-v1` English + Bangla, all teams
  (amber banner + `/how-to`). Emails Stitch, Production, QC and the
  other floor mailboxes. No passwords.
- Confirm Stop on an already-closed session: acknowledge, optionally
  clamp `ended_at` to the requested stop. Do not 409
  "Session is no longer open; cannot stop."
- `eebe8653` did not go Ready: Next webpack rejected
  `generate-attendance-wall-qr-pdf.ts` (`stream did not contain valid
  UTF-8`, Latin-1 middle-dot `0xB7`). Live stayed on `41896963`.
- Ship: `e836a745`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
  How-to emails on first ERP login after Ready (`/api/team-notices`).
- Floor now print control was a small white button; amber bar + dark
  **Print attendance QR** so it is visible on Production.
  Ship: `b7df706d`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Tablet camera on Stitch kiosk (`Use tablet camera`) for badge + wall
  QR (and A4). Same queue as the USB gun. Chrome on the tablet. Gun
  still works. Production pipeline badge box stays gun-only.
  Ship: `dc438e47`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Owner: after attendance they may not start stitching. Clock-in at
  the entrance only. It must not leave a badge arm. Later they scan
  badge again + garment A4 to start a piece. How-to
  `howto-wall-attendance-qr-v2`. Reprint the wall poster.
  Ship: `7665318b`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Owner: badge then wall QR, or wall QR then badge - both must register.
  Wall first now waits (does not 409). Completing the pair writes
  `stitch_attendance`. Still no stitch arm. How-to
  `howto-wall-attendance-qr-v3`. Reprint the wall poster.
  Ship: `a0eef96a`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).

## Cost hint worksheet PDF

- Owner: print a PDF of hint cost to work offline. Sitewide.
  Admin Costing + Invoicing + each invoice. Internal only - not the
  client invoice PDF. Blank Write price column. Missing catalog prices
  stay "-".
  Print `/costing/print`. Download `GET /api/costing/hint-pdf`.
  Ship: `ab9e3c6e`. Both Vercel Ready (`garment-erp`, `garment-erp-kvsf`).
- Sitewide header: **Cost hints** on every admin page (Dashboard, Sales
  Home, Sales Orders list + detail, Invoicing). Same PDF. Still
  `canViewMoney` only. Not on the client invoice PDF.
