# Session 2026-08-17

## Inventory: recipe coverage for all live garment types

- Gap found: 11 garment types appearing in live sales orders had NO hanger
  recipe, so packing them would deduct nothing - including Overshirt (237
  lines) and Short (128 lines).
- Seeded by analogy to the owner's Aug 16 seed (suit hanger for
  trouser/jacket-like garments, laundry hanger for shirt-like):
  - 1 suit hanger: Overshirt, Short, Vest, Salwar, Dagla
  - 1 laundry hanger: T-shirt, House Thobe, Hoodie
  - 1 laundry + 1 suit: Shirt+Short, Thobe+Jacket
  - 1 laundry + 2 suit: Shirt+Trouser+Short
- Skipped on purpose: "Fabric only" (no garment) and "Boxers" (folded, no
  hanger). Every other garment type in live orders now has a recipe
  (20 recipes total). Owner can adjust any recipe in /inventory.
- Written to Supabase `inventory_store` + bundled fallback refreshed.
- Still needed from the floor: opening stock counts - both hangers sit at
  0 pcs with an empty ledger, so on-hand numbers go negative until the
  team counts and enters real stock (negative stock is allowed by design,
  the floor is never blocked).

## Inventory: belt fabric items (owner ask)

- Six belt-fabric items added (unit: cm, category "Belt fabric") - the
  waistband lining used inside the trouser at belt level:
  - Fouad Rhame brand: White, Black
  - Gliani brand: White, Black
  - Plain (no label): White, Black
- Consumption per trouser (cm) still to come from the owner; recipes not
  set yet. Open modeling question for when it arrives: a trouser uses ONE
  of the six belt fabrics (brand + color chosen per client/order), but
  garment-type recipes deduct a fixed item list per garment type - the
  packed-stage deduction cannot know which belt fabric a given trouser
  used. Likely needs either a default item + manual switch, or a picker
  at pack time.

## Inventory: suit hangers split by brand (owner ask)

- "Suit hanger" is now three items: Fouad Rhame, Gliani, and White label
  (no brand). The existing `inv-suit-hanger` item kept its id and became
  the White label one, so all 15 garment recipes that deduct a suit
  hanger keep working and default to White label. Same brand-pick
  question as belt fabric applies when packing branded garments.
- Laundry hanger left as a single unbranded item (owner asked only for
  suit hangers).
- Same split added for Suit bags and Waist belts (Fouad Rhame / Gliani /
  White label, pcs, low-stock 10). Inventory now has 16 items across
  Hangers, Belt fabric, Suit bags, Waist belts. None are wired to
  recipes yet except the hangers - suit bag / waist belt / belt fabric
  consumption rules await the owner's numbers and the brand-pick
  decision.

## Inventory: brand field + filter (owner ask)

- `InventoryItem.brand` added as a real field (optional for pre-Aug-17
  items; null = brandless consumable like the laundry hanger). Item
  create/update API accepts it; the add-item form has a Brand input with
  a datalist of known brands.
- Stock table has brand filter pills (All / each brand / No brand) and a
  Brand column. Filter is client-side on the stock table only - recipes
  and ledger stay unfiltered.
- All 16 existing items backfilled in Supabase from their names
  (Fouad Rhame / Gliani / White label; laundry hanger stays brandless).
